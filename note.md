# EVSOD 实验日志

> 本文件只保留在本地，不上传 Git。它是后续优化的连续记忆：每次实验必须记录动机、训练配置、
> 完整验证分数、融合结果和结论。公开复现以 `README.md` 为准。

## 当前状态

- 当前本地验证最高分：**M26 epoch 003 + P18-global + P41（高密度相位集成），Score = 0.9638562171**。
- 验证集：EV-UAV Challenge 2 的 `val/`，共 24 个视频。
- 当前生产推理：`event_count <= 30000` 使用 M10 epoch 002；其余视频使用 M26 epoch 003。
- 当前固定后处理：P6 `0.718 / 0.7226`、P0/P0c、P18-global（无 event_count 上限）；P41 仅在
  `event_count > 30000` 时启用，使用半箱偏移 25 和原流权重 `0.75`。
- 直接复现命令、checkpoint checksum 和提交命令写在 `README.md` 的
  **免训练评估** 一节。

当前最佳完整指标：

```text
Score = 0.9638562171
Pd    = 0.9785804284
IoU   = 0.9423022866
Acc   = 0.9763227701
Fa    = 4.6632902944e-06
```

## 固定评估协议

所有模型选择必须使用下列完整流程，不能按训练 loss、局部样本或缓存扫描的绝对分数选模型。

1. 用 `test2.py` 在全部 24 个验证视频上推理。
2. 低密度路由固定为 M10：`event_count <= 30000`。
3. 高密度模型使用待验证 checkpoint；若模型含 attention，必须设置
   `TEMPORAL_MEMORY.temporal_memory_temporal_attention_enabled=true`。
4. 固定 P6：低密度阈值 `0.718`，其他视频阈值 `0.7226`。
5. 固定 P0/P0c：`spatial_radius=2`、`temporal_radius_bins=1`、`min_cluster_events=3`、
   `min_duration_bins=5`、`retain_min_score=0.95`。
6. 固定 P18-global：`max_event_count=0`（无上限）、`candidate_floor=0.53`、`spatial_radius=5`、
   `max_link_distance=8.0`、`min_track_bins=4`。
7. 记录 `Score/Pd/IoU/Acc/Fa`。若要采用融合方案，缓存扫描只用于筛选，最终必须再跑一次
   完整 `test2.py`。

评分公式：

```text
Score_Fa = exp(-10000 * Fa)
Score = 0.4 * Pd + 0.3 * Score_Fa + 0.2 * IoU + 0.1 * Acc
```

## 历史主线

| 阶段 | 主要改动 | 最佳本地 Score | 结论 |
| --- | --- | ---: | --- |
| P23 | 官方全事件流时序帧基线 | 0.9382006487 | 证明完整事件流时序帧路线有效 |
| M4 + DACC | 双向时序记忆与密度自适应通道校准 | 0.94822 | 明显优于 P23 |
| M4 + DACC + M5 | 加入轨迹外推训练损失 | 0.94965 | 有基础增益，但后续不宜过强使用轨迹约束 |
| P0 调优 | 时空连通簇过滤与高置信恢复 | 0.95444 | 后处理是稳定增益来源 |
| M6/M9 | 短程微调与低密度 P18 | 0.95780 / 0.95793 | 低密度轨迹恢复有效 |
| M10 | 高密度采样专家 | 0.95844 | 后续固定为低密度路由模型 |
| M11 + M10 | 提高高密度采样并按事件数路由 | 0.95929 | 输入事件数是可泛化的路由依据 |
| M13 | 高密度连续视图重采样 | 0.9595899017 | 确认高密度视频应增加训练视图 |
| M13-FT30 | M13 epoch 003 的低学习率长程续训 | 0.9609920008 | 训练早期出现局部峰值，后续会退化 |
| M15 | 从 M13-FT30 epoch 003 低学习率续训 | 0.9623923830 | 时序记忆主干的关键稳定版本 |
| M16 | M15 的极低学习率续训，与 M15 融合 | 0.9625116616 | 弱单模型也可能提供互补误差 |
| M17 | M15 + 8 倍高密度采样、seed 45 | 0.9625205670（融合） | 微小融合收益，需谨防验证集过拟合 |
| M18 | M17 同配方、seed 46 | 0.9626372379（融合） | M15/M18 是 M20 之前最佳 |
| M19 | M15 + 8 倍高密度采样、seed 47 | 0.9619559819 | 仅换 seed 不能稳定提分 |
| M20 | M15 上新增零初始化时序自注意力 | **0.9628776542** | 当前最佳，首次超过 M15/M18 融合 |
| M21 | M20 同配方、seed 49 | 0.9626722491 | 未超过 M20，且融合无收益 |
| M22 | attention 学习率提高到 4 倍 | 0.9626048149 | 局部峰值仍低于 M20，融合无收益 |
| M23 | attention 输出投影 `std=1e-3` 非零初始化 | 0.9619527383 | 未恢复 M20 的召回跃升，融合无收益 |

## 已确认的有效经验

### 1. 全事件流时序表征优于固定预算稀疏推理

M4 之后的核心路径始终是完整事件流按时间箱构造输入帧，在每个原始事件坐标提取连续分数。
高密度视频若被固定事件预算截断，会丢失目标上下文；当前方案不再依赖该截断路径。

### 2. 高密度训练采样是持续有效的增益点

当 `event_count > 200000` 时，把训练视图数提高到 8 能覆盖更多时段。M13、M17、M18、M19、
M20、M21 都沿用了这个设置。只依赖输入事件数，不依赖验证标签。

### 3. 训练 loss 不能用于 checkpoint 选择

- M13-FT30 的最佳完整验证出现在 epoch 003，继续至 30/50 轮没有收益。
- M15 的训练 loss 最优并非完整验证最优。
- M20 的训练 loss 最优是 epoch 011，而完整验证最优是 epoch 003。

今后只要训练保存了多轮 checkpoint，就应优先完整验证前期和中期 checkpoint；看到 loss 继续下降
不能推断 Challenge 2 Score 会继续提高。

### 4. 后处理和阈值必须随模型固定验证

当前 P0/P0c/P18/P6 组合对 M20 最好。P18 的候选下限、连接距离、最少时间箱和阈值都曾扫描过；
改变模型后可以小范围复查，但不能把缓存评估的绝对分数当成正式结论。

### 5. 融合只接受完整验证增益

M16/M17/M18 曾依靠与 M15 的误差互补得到小幅提升。M20 出现后，M15/M20、M18/M20、
M15/M18/M20 的定向扫描均未超过 M20 单模型，因此当前不使用融合。融合会增加推理成本，也更容易
对 24 视频验证集的离散决策过拟合。

## M20：当前最佳实验

### 动机

双向 ConvGRU 通过递归传播时间证据，但远距离时间箱之间需要多步传递。M20 在 bottleneck 上加入
多头时序自注意力：每个空间位置的所有时间 token 可直接交互。attention 输出投影零初始化，加载
M15 时先保持原预测，再逐步学习残差。

### 训练配置

```text
初始化：checkpoints/m15_e3_low_lr_epoch_008_seed43.pt
seed：48
epoch：12
学习率：1e-6 -> 1e-7（cosine）
高密度采样：event_count > 200000，dense_view_multiplier=8
attention：启用
metric auxiliary：关闭
trajectory extrapolation：关闭
```

训练目录：

```text
log/m20_attn_dense_views8_e12_seed48/runs/20260809-102413_seed48_pid23008/
```

最终采用权重：

```text
checkpoints/m20_attn_dense_views8_epoch_003_seed48.pt
SHA-256: 4B8B2B19EA9D913EE4E52CB21AE52BF945B2B0F3CEFD5CB5AB6F64D51BF49849
```

### M20 结论

M20 epoch 003 将此前 M15/M18 融合的 `0.9626372379` 提高 `0.0002404163`。M20 第 4 轮以后
验证分数没有继续提高，训练到第 12 轮也没有产生更高 checkpoint；因此不再从 M20 同一路径直接延长
训练轮数。

完整验证日志：

```text
log/analysis/m20_epoch001_final_verify.log ... m20_epoch012_final_verify.log
```

## M21：独立 attention seed 验证（未采用）

### 目的与配置

排除 M20 只是 seed 48 偶然峰值的可能。M21 与 M20 相同，区别仅为训练采样 seed 49：

```text
初始化：M15 epoch 008
seed：49
epoch：12
学习率：1e-6 -> 1e-7（cosine）
dense_view_multiplier：8
attention：启用
```

训练目录：

```text
log/m21_attn_dense_views8_e12_seed49/runs/20260809-145825_seed49_pid24892/
```

### 全部 checkpoint 完整验证

| M21 checkpoint | Score |
| --- | ---: |
| epoch 001 | 0.9619794292 |
| epoch 002 | 0.9619745064 |
| epoch 003 | **0.9626722491** |
| epoch 004 | 0.9619064403 |
| epoch 005 | 0.9619512965 |
| epoch 006 | 0.9619130788 |
| epoch 007 | 0.9619958322 |
| epoch 008 | 0.9619378309 |
| epoch 009 | 0.9619014380 |
| epoch 010 | 0.9618939118 |
| epoch 011 | 0.9613383500 |
| epoch 012 | 0.9613399048 |

M21 epoch 003 指标：

```text
Score = 0.9626722491
Pd    = 0.9762704746
IoU   = 0.9414162636
Acc   = 0.9765975475
Fa    = 4.7018053985e-06
```

它比 M20 epoch 003 低 `0.0002054051`，不采用。

### M20/M21 融合验证

先生成 M20/M21 + M10 低密度路由的原始分数缓存，再扫描：

```text
M21 权重：0.05 到 0.95
高密度阈值：0.7185 到 0.7210
P0/P0c/P18：保持固定协议
```

最佳候选为 `5% M21 + 95% M20`、高密度阈值 `0.719`，结果恰好仍为
`0.9628776542`，没有超过 M20 单模型。因此不需要额外完整验证，也不更新 README。

相关本地文件：

```text
log/analysis/m21_epoch001_final_verify.log ... m21_epoch012_final_verify.log
log/analysis/m21_e3_cache_focused.log
log/analysis/m20_m21e3_fine_blend.log
```

## 已淘汰或暂不继续的方向

| 方向 | 结果 | 暂停原因 |
| --- | --- | --- |
| M13-FT30 继续到 50 轮 | 无法超过早期 epoch 003 | 明显的早期峰值和后续漂移 |
| M14 | 未继续 | M13 之后已转向更有效的 M15 低学习率续训 |
| M19 纯 ConvGRU 独立 seed | 最佳 0.9619559819 | 低于 M15/M18 与 M20 |
| M20 同配方长训 | epoch 003 后未提升 | 无支持证据继续增加轮数 |
| M21 仅更换 attention seed | 最佳 0.9626722491 | 低于 M20，融合也不增益 |
| M22 attention 学习率 4 倍 | 最佳 0.9626048149 | 未改善 M20 的 IoU/Fa 组合，融合无收益 |
| M23 attention 投影非零初始化 | 最佳 0.9619527383 | 全程 Pd 未提升，融合仍低于 M20 |
| M15/M20、M18/M20、三专家融合 | 未超过 M20 | 单模型更好且推理更简单 |

## 下一步研究假设

M22/M23 表明直接改变 attention 的优化速度或用非零投影让它从第一步获得梯度，都没有恢复 M20 epoch 003 的 Pd 跃升。M24 的相对时间偏置在实现后经误差归因否决，原因见后文“**M24 复盘**”。下一项优先方向确定为 **M25：跨空间平流对齐**。它以相邻 bottleneck 的局部卷积特征估计稠密二维位移，并把 ConvGRU 隐状态 warp 到当前时间箱；这样移动目标不必在同一空间坐标才可使用前一箱的记忆证据。

M25 必须从 M20 epoch 003 挂接，并令新 flow head 的输出层为零初始化。训练开始前 flow 为零、warp 是精确恒等，因此预测逐位等于当前最优 M20；任何后续变化均可归因于平流分支，而非重新初始化或改写既有 attention。

## M22：attention 独立学习率（不采用）

动机：M20/M21 的 attention 输出投影零初始化，attention 内部参数在早期受到梯度抑制；将其与 ConvGRU 共用 `1e-6` 可能导致分支学习不足。M22 保持 base 与 ConvGRU 学习率不变，仅将 attention 学习率提高到 `4e-6`。

代码改动：`train_temporal_memory.py` 新增 `attention` optimizer group；配置新增 `TEMPORAL_MEMORY.temporal_memory_attention_lr_multiplier`，默认 `1.0` 以保持旧实验兼容。

训练配置：从 `checkpoints/m15_e3_low_lr_epoch_008_seed43.pt` 初始化，seed 50，16 epochs，dense view multiplier 8，global LR `1e-6 -> 1e-7` cosine，attention multiplier `4.0`。训练输出目录为 `log/m22_attn_lr4_dense_views8_e16_seed50/runs/20260810-091320_seed50_pid967/`。

完整验证结果：

| epoch | Score | Pd | IoU | Acc | Fa |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 001 | 0.9619643049 | 0.9745905082 | 0.9408300519 | 0.9755442142 | 4.6366259916e-06 |
| 002 | 0.9622315796 | 0.9745905082 | 0.9417659044 | 0.9756663442 | 4.6129243890e-06 |
| 003 | 0.9619469061 | 0.9745905082 | 0.9409764409 | 0.9759258628 | 4.6662529947e-06 |
| 004 | 0.9617364762 | 0.9745905082 | 0.9407697320 | 0.9761853814 | 4.7343951019e-06 |
| 005 | 0.9624386971 | 0.9762704746 | 0.9410267472 | 0.9765670300 | 4.7551340041e-06 |
| 006 | **0.9626048149** | 0.9762704746 | 0.9417496920 | 0.9766128063 | 4.7492086035e-06 |
| 007 | 0.9618835118 | 0.9745905082 | 0.9416336417 | 0.9765212536 | 4.7551340041e-06 |
| 008 | 0.9618737105 | 0.9745905082 | 0.9413236976 | 0.9759411216 | 4.7166189000e-06 |
| 009 | 0.9617414554 | 0.9745905082 | 0.9412344694 | 0.9763227701 | 4.7699475057e-06 |
| 010 | 0.9617264858 | 0.9745905082 | 0.9411435723 | 0.9761853814 | 4.7640221051e-06 |
| 011 | 0.9617197160 | 0.9745905082 | 0.9411444664 | 0.9762006402 | 4.7669848054e-06 |
| 012 | 0.9616993903 | 0.9745905082 | 0.9411470294 | 0.9762464762 | 4.7758729063e-06 |
| 013 | 0.9617229678 | 0.9745905082 | 0.9411920309 | 0.9763075113 | 4.7729102060e-06 |
| 014 | 0.9617385446 | 0.9745905082 | 0.9412664771 | 0.9763991237 | 4.7758729063e-06 |
| 015 | 0.9616886334 | 0.9745905082 | 0.9411669374 | 0.9763532877 | 4.7847610073e-06 |
| 016 | 0.9616841630 | 0.9745905082 | 0.9411522150 | 0.9763380289 | 4.7847610073e-06 |

epoch 006 是 M22 最佳，但比 M20 `0.9628776542` 低 `0.0002728393`；M20/M22 缓存融合扫描最高仍为 `0.9628776542`（`98% M20 + 2% M22`），不采用。相关日志：`log/analysis/m22_epoch001_final_verify.log` 至 `m22_epoch016_final_verify.log`、`log/analysis/m20_m22e6_fine_blend.log`。

## M23：attention 输出投影小幅非零初始化（不采用）

动机：M20/M22 的 attention 输出投影为零时，第一步反向传播不会更新 Q/K/V。M23 将该投影初始化为均值 0、标准差 `1e-3`，让 attention 内部参数从第一步获得梯度，同时使新增残差保持极小。

训练配置：从 `checkpoints/m15_e3_low_lr_epoch_008_seed43.pt` 初始化，seed 51，12 epochs，dense view multiplier 8，global LR `1e-6 -> 1e-7` cosine，attention multiplier `1.0`，输出投影初始化标准差 `1e-3`。训练目录为 `log/m23_attn_smallinit1e3_dense_views8_e12_seed51/runs/20260810-122232_seed51_pid2639/`。

完整验证结果：

| epoch | Score | Pd | IoU | Acc | Fa |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 001 | 0.9618704156 | 0.9745905082 | 0.9405208826 | 0.9752236605 | 4.6366259916e-06 |
| 002 | **0.9619527383** | 0.9745905082 | 0.9409080148 | 0.9756968617 | 4.6514394931e-06 |
| 003 | 0.9618740190 | 0.9745905082 | 0.9408197999 | 0.9758495688 | 4.6781037960e-06 |
| 004 | 0.9617511166 | 0.9745905082 | 0.9407852888 | 0.9762158990 | 4.7314324016e-06 |
| 005 | 0.9618859340 | 0.9745905082 | 0.9414051175 | 0.9761548638 | 4.7255070010e-06 |
| 006 | 0.9618638990 | 0.9745905082 | 0.9414068460 | 0.9761853814 | 4.7343951019e-06 |
| 007 | 0.9618884132 | 0.9745905082 | 0.9415107369 | 0.9763075113 | 4.7373578022e-06 |
| 008 | 0.9618753279 | 0.9745905082 | 0.9415419102 | 0.9763686061 | 4.7462459032e-06 |
| 009 | 0.9618557478 | 0.9745905082 | 0.9414821863 | 0.9762922525 | 4.7462459032e-06 |
| 010 | 0.9618942429 | 0.9745905082 | 0.9414466619 | 0.9761548638 | 4.7255070010e-06 |
| 011 | 0.9617315630 | 0.9745905082 | 0.9411418438 | 0.9761548638 | 4.7610594047e-06 |
| 012 | 0.9617044606 | 0.9745905082 | 0.9411453009 | 0.9762158990 | 4.7729102060e-06 |

最佳 epoch 002 比 M20 低 `0.0009249159`，且 12 个 checkpoint 的 Pd 均为 `0.9745905082`，没有出现 M20 epoch 003 的 `0.9762704746` 召回跃升。M20/M23 原始分数融合扫描的最佳候选为 `92% M20 + 8% M23`，Score 为 `0.9628523726`，仍比 M20 单模型低 `0.0000252816`，不采用。

相关日志：`log/analysis/m23_epoch001_final_verify.log` 至 `m23_epoch012_final_verify.log`、`log/analysis/m23_e2_m10low30000_raw_scores.pt`、`log/analysis/m20_m23e2_fine_blend.log`。

## M24 复盘：相对时间偏置不训练

M24 原计划在 M20 的同一空间位置时序 attention 上添加方向和距离感知的相对时间偏置。实现已通过零残差、M15 安全挂接和 checkpoint 加载冒烟检查，但在训练前被误差归因否决，不投入长训练。

依据如下：

1. M20 固定策略下共有 4,765 个官方目标时间窗，其中 84 个（1.76%）是原始分数不足，29 个（0.61%）是后处理删除；P0c 保留阈值从 `0.70` 到 `0.95` 的缓存扫描确认 `0.95` 仍是唯一最优，不能靠简单放宽后处理恢复这些目标。
2. 原始低分漏检目标的中位运动速度为 `4.436 px/50 时间单位`，已检测目标仅为 `1.929 px/50 时间单位`；后处理删除组也达到 `3.406 px/50 时间单位`。漏检与快速跨空间位移显著相关。
3. 最大误差来自高密度 `val_023`：413 个目标时间窗中有 32 个原始低分漏检，另有 4 个被后处理删除。M24 的静态时间偏置不会改变 attention 只在同一空间位置取值的限制，无法对齐运动目标。
4. M15 到 M20 的逐目标比较中，M20 仅多命中 1 个目标时间窗、没有新增漏检；M20 的分数优势主要来自 IoU/Fa 的小幅校准。因此 M21/M22/M23/M24 这类同分支微调的预期收益低于当前验证噪声量级。

结论：**不训练 M24**。后续若继续训练，优先尝试从 M20 epoch 003 零残差挂接跨相邻时间箱、跨空间邻域的局部运动对齐分支：在 bottleneck 的 3x3 邻域内为前后帧寻找匹配证据，直接解决快速目标不再位于同一空间位置的问题。该方案必须保持 M20 初始输出不变，并以完整 `test2.py` 验证。

相关本地分析：`log/analysis/m20_error_attribution.log`、`log/analysis/m20_p0c_recovery_scan.log`、`log/analysis/m15_m20_target_window_comparison.log`。

## M25：跨空间平流对齐（待训练）

### 方向来源与取舍

队友 `Ice7en/Jinzhongzi` 的 `m10-p18` 分支在其 `0.95444` 版本上加入 M10 平流对齐后达到 `0.95674`；其后的 P18 达到 `0.95920`。本项目已经拥有更强的 P18 和 M20，不能将绝对分数横向相加，但 M10 的机制正好对应本项目的误差归因：原始低分漏检的中位运动速度为 `4.436 px / 50`，而已检出目标仅为 `1.929 px / 50`。

因此 M25 采用该机制，而不再训练 M24 的同空间时间偏置。它只对相邻时间箱操作，不引入全视频跨空间 attention 的显存开销；flow head 是两层局部卷积后接零初始化的二维位移输出。

### 实现与安全检查

- `model/temporal_memory_net.py`：新增 `FlowEstimationHead`、`advect_warp` 与双向 ConvGRU state 的平流。训练期新增自监督 `mse(warp(feat_prev, flow), feat_curr.detach())`。
- `train_temporal_memory.py`：flow head 独立 optimizer group；M20 加载时仅允许新增的 7 个 `flow_head.*` 参数缺失；checkpoint 保存 M25 结构元数据。
- `utils/temporal_memory_inference.py`：根据 checkpoint 元数据恢复 M25 结构，保持旧 M20 checkpoint 严格加载。
- 已用 M20 epoch 003 验证：仅 flow head 的 7 个参数可缺失；初始 flow 全零；M20 与已挂接 M25 的 temporal residual **逐位相等**；平流一致性损失对零初始化输出层有非零梯度；旧 M20 推理加载回归通过。

PyTorch 1.9 的 identity `grid_sample` 存在约 `2.7e-6` 的插值舍入误差。实现中只在 flow 输出层仍严格为零时采用保梯度的精确恒等前向，避免训练开始前改变 M20 的任何预测；首个更新后自动改回常规双线性 warp。

### 首轮训练设计

- 初始化：`checkpoints/m20_attn_dense_views8_epoch_003_seed48.pt`。
- 保持 M20 的高密度 8 倍确定性采样、DACC、关闭 metric/trajectory 辅助项及固定后处理协议。
- 全局学习率 `1e-6 -> 1e-7` cosine，训练 16 epoch 并每轮保存。M20 的峰值常出现在早期，但新 flow 输出层第一步才解锁内部层，保留 16 轮以覆盖该启动期，不据训练 loss 选模型。
- 已有 base、ConvGRU、attention 使用 `0.25x` 学习率；新 flow head 使用 `1.0x`；平流一致性权重采用队友已扫描最优的 `0.05`。这是保守迁移，不保证该超参在 M20 上同样最优。
- 每个 epoch 均须按固定 M10 低密度路由 + 当前 P6/P0/P0c/P18 协议完整 `test2.py` 验证。仅当 Score 严格超过 `0.9628776542` 时才更新 README、发布 checkpoint 或生成新提交。

### 完整验证结果

训练目录：`log/m25_advect_m20e3_dense8_e16_seed52/runs/20260810-192134_seed52_pid539/`。所有 checkpoint 均按固定 M10 低密度路由、P6 `0.718 / 0.719`、P0/P0c `0.95`、P18 几何参数完整 `test2.py` 验证。

| epoch | Score | Pd | IoU | Acc | Fa |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 001 | **0.9627936608** | 0.9760604788 | 0.9422507882 | 0.9766433835 | 4.6899545972e-06 |
| 002 | 0.9627554162 | 0.9760604788 | 0.9421944022 | 0.9766281247 | 4.6988426982e-06 |
| 003 | 0.9627344558 | 0.9760604788 | 0.9422134161 | 0.9767196774 | 4.7106934994e-06 |
| 004 | 0.9627708360 | 0.9760604788 | 0.9423147440 | 0.9767960310 | 4.7077307991e-06 |
| 005 | 0.9627904221 | 0.9760604788 | 0.9423745275 | 0.9768723249 | 4.7077307991e-06 |
| 006 | 0.9627814814 | 0.9760604788 | 0.9423450828 | 0.9768418074 | 4.7077307991e-06 |
| 007 | 0.9627814814 | 0.9760604788 | 0.9423450828 | 0.9768418074 | 4.7077307991e-06 |
| 008 | 0.9627859517 | 0.9760604788 | 0.9423598051 | 0.9768570662 | 4.7077307991e-06 |
| 009 | 0.9627814814 | 0.9760604788 | 0.9423450828 | 0.9768418074 | 4.7077307991e-06 |
| 010 | 0.9625281059 | 0.9760604788 | 0.9419453740 | 0.9768875837 | 4.7699475057e-06 |
| 011 | 0.9625234904 | 0.9760604788 | 0.9419764876 | 0.9769486785 | 4.7758729063e-06 |
| 012 | 0.9624965301 | 0.9760604788 | 0.9419340491 | 0.9769334197 | 4.7817983069e-06 |
| 013 | 0.9625211920 | 0.9760604788 | 0.9419921041 | 0.9769791961 | 4.7788356066e-06 |
| 014 | 0.9625228847 | 0.9760604788 | 0.9419929385 | 0.9769944549 | 4.7788356066e-06 |
| 015 | 0.9625279607 | 0.9760604788 | 0.9419912100 | 0.9769639373 | 4.7758729063e-06 |
| 016 | 0.9624931474 | 0.9760604788 | 0.9419366121 | 0.9769791961 | 4.7847610073e-06 |

M25 最好为 epoch 001，仍比 M20 `0.9628776542` 低 `0.0000839934`，不采用。第 12 轮为训练 loss 最低点，却是验证后期低点之一，再次确认 loss 不能选择 Challenge 2 checkpoint。

### 误差归因与否决结论

1. **Pd 没有改善。** M25 的 16 个 checkpoint 均为 `0.9760604788`，M20 为 `0.9762704746`。逐目标对照显示 4,765 个目标时间窗中，M20 独有 1 个命中、M25 没有独有命中；该项位于 `val_020.npz`、`target_id=2`、time bin 146。
2. **唯一丢失项不是原始置信度变低。** 该目标窗的峰值分数 M20 为 `0.708709`、M25 为 `0.727759`，但 M25 的高分小组件被 P0 删除且没有被 P18 恢复。M25-e1 的 P0c 保留阈值从 `0.70` 到 `0.95` 回放后，`0.95` 仍唯一最优；放宽到 `0.70` 虽将 Pd 提升至 `0.9825703486`，Fa 同时升至 `8.3222251823e-06`，Score 降至 `0.9516979199`。因此不修改生产后处理。
3. **flow 的实际尺度不足。** 在误差最大的 `val_023`，M25-e1 的 flow 大小（bottleneck 单元）为 p50 `0.00655`、p99 `0.01295`，折合输入像素 p99 仅 `0.104`；epoch 16 也仅为 `0.328` 输入像素。验证集目标质心相邻箱运动的 p90/p95/p99 为 `4.06 / 4.92 / 8.30` 输入像素，即 p99 约 `1.04` bottleneck 单元。当前 self-supervised feature consistency 被静态背景主导，学到的移动量至少比目标需要的尺度小一个数量级。
4. **融合无价值。** M20/M25-e1 原始分数缓存使用生产 P18 协议扫描 M20 主权重 `0.70–0.98` 和高密度阈值 `0.7185–0.7210`，最优仍是 `0.98 M20`、高密度阈值 `0.719`，精确等于 M20 单模型 `0.9628776542`。

结论：**M25 不采用，也不增加训练轮次。** 它证明跨空间平流方向与问题相关，但当前纯特征一致性损失无法使 flow 达到真实目标速度。

### M26 候选：目标轨迹约束的可尺度平流

后续应保留 M20 作为严格零扰动初始化，在 M25 flow head 上新增训练期的目标运动监督：按连续时间箱内相同 `target_id` 的正事件质心计算位移，在当前目标质心位置采样 flow，使用 Huber 损失监督其指向上一时间箱位置（warp 需要的逆位移）。同时将 raw flow 映射为有上限的 `max_flow * tanh(raw_flow)`，建议上限先取 `2` 个 bottleneck 单元（约 16 输入像素），覆盖验证运动 p99 又不允许异常大位移。零初始化时仍严格输出零，因此 M20 初始预测不变。

该方向与 M25 的区别是：M25 只要求全图特征在极小位移下相似；M26 直接告诉 flow 目标在哪里、应移动多远，并把监督仅作用于训练集已有标注。实现后须先验证监督符号、尺度转换、零扰动加载和一个 batch 的非零 flow 梯度，再决定是否训练。

### M26 实现与训练前验证

已实现但尚未开始长训练。M26 继续从 M20 epoch 003 挂接 `FlowEstimationHead`，新增两项训练期约束：

1. 输出位移改为 `max_flow * tanh(raw_flow)`，本轮采用 `max_flow=2.0` 个 bottleneck 单元（约 16 输入像素）。这样既覆盖验证集目标相邻箱运动的 p99，又避免无界 flow 破坏记忆状态。
2. 对相邻时间箱中同一 `target_id` 的正事件，计算各自质心；在当前质心采样 flow，并以 Huber loss 监督它指向上一箱质心。由于 warp 在当前坐标采样 `current + flow`，监督目标是 `previous_center - current_center`，即逆位移。

已完成的检查：

- 四个涉及 M26 的 Python 文件通过 `py_compile`。
- 将 M20 epoch 003 加载到 M26 时，缺失键严格只有 7 个 `flow_head.*` 参数；没有 unexpected key。
- 零初始化 flow 的最大绝对值为 0，M20 与 M26 的随机 bottleneck temporal residual 用 `torch.equal` 逐元素相等。
- 合成匀速轨迹有 4 对监督项，目标 flow loss 为 `0.2427465`，flow 输出层权重和偏置梯度均非零。
- 真实训练序列（16 个时间箱、4362 个事件）有 15 对同目标相邻轨迹，目标运动均值为 `0.19184` 个 bottleneck 单元，目标 loss 为 `0.0094485`，flow 输出层梯度非零。
- M20 checkpoint 的 `density_calibration_enabled=true`，而 YAML 默认是 false；M26 命令必须显式设置 `TEMPORAL_FRAME.temporal_frame_density_calibration_enabled=true`，否则架构校验会拒绝加载。

首轮训练固定 12 epoch、每轮保存，从 M20 epoch 003 初始化。保持 M20 的 dense 8x 采样、全局 LR `1e-6 -> 1e-7` cosine；base / ConvGRU / attention 使用 `0.25x`，flow head 使用 `4x`；feature consistency 权重降低到 `0.01`，target-flow Huber 监督权重为 `0.5`。

### M26 训练与验证结果（采用）

训练目录：`log/m26_targetflow_m20e3_dense8_e12_seed53/runs/20260810-234032_seed53_pid2826/`。训练 loss 最低为 epoch 011 的 `0.0354595`，但 Challenge 2 最优发生在 epoch 003，不能按 loss 选择。

按固定 M10 低密度路由、P0/P0c/P18 协议验证的代表性 checkpoint：

| epoch | Score（高密度阈值 0.719） | Pd | 结论 |
| ---: | ---: | ---: | --- |
| 001 | 0.9621064307 | 0.9748005040 | 早期退化 |
| 002 | 0.9621590378 | 0.9748005040 | 退化 |
| 003 | 0.9628418268 | 0.9764804704 | 唯一接近 M20 的峰值 |
| 004 | 0.9620316588 | 0.9748005040 | 退化 |
| 005 | 0.9619831376 | 0.9748005040 | 退化 |
| 007 | 0.9614094133 | 0.9735405292 | 明显退化 |
| 011 | 0.9620414397 | 0.9752204956 | 最低 loss 但验证较低 |

epoch 003 的 Pd 比 M20 高 `0.0002099958`，但默认高密度阈值 `0.719` 时 IoU 更低，单模型仍比 M20 低 `0.0000358274`。因此不增加训练轮次，而是在已有原始分数缓存上检查校准：

1. M20/M26-e3 概率融合扫描无收益，最佳仍为 M20；M26 的新增召回在混合后没有稳定保留。
2. P18 几何参数和 P0c 保留阈值扫描无收益，P0c `0.95` 仍是 M26 最优。
3. 仅将高密度 P6 阈值细扫到 `0.7216–0.7240` 后，`0.7226` 为最优。低密度 M10 阈值保持已验证的 `0.718`。

最终以完整 `test2.py` 独立复核：

```text
checkpoint: log/m26_targetflow_m20e3_dense8_e12_seed53/runs/20260810-234032_seed53_pid2826/epoch_003_seed53.pt
P6: event_count <= 30000 -> 0.718; otherwise -> 0.7226
IoU:      0.9418393970
Acc:      0.9759869576
Pd:       0.9764804704
Fa:       4.6484767928e-06
Score_Fa: 0.9545791007
Score:    0.9629324935
```

它比 M20 `0.9628776542` 高 `0.0000548393`，因此采用。发布 checkpoint 为 `checkpoints/m26_targetflow_m20e3_epoch_003_seed53.pt`，SHA-256 为 `13F7D4D8AB6BDCAAA98F3F906A7D32E687C17454B88B42B94752EEC04257F7C4`。后续研究应从 M26 的 `epoch 003` 出发，而不是继续训练其 epoch 011 以后的漂移分支。

### P18-global：取消上限门控的全视频轨迹恢复（采用，未提交 Git）

M26 的原始 P18 只对 `1 < event_count <= 35000` 的视频启用。该上限来自早期模型的验证扫描，
但 M26 的误差归因表明高密度快速目标仍是主要漏检来源，因此对同一套无标签规则进行了重新验证：
保持 `candidate_floor=0.53`、`spatial_radius=5`、`max_link_distance=8`、`min_track_bins=4` 和
`restore_mode=best` 不变，只将 `p18_max_event_count` 从 `35000` 改为 `0`（无上限）。

完整 `test2.py` 独立复核：

```text
M26 epoch 003 + M10 low-density route
P6: event_count <= 30000 -> 0.718; otherwise -> 0.7226
P18: event_count > 1, no upper event-count gate
IoU:      0.9415289760
Acc:      0.9761396050
Pd:       0.9785804284
Fa:       4.7462459032e-06
Score_Fa: 0.9536462733
Score:    0.9634458090
```

相对 M26 原固定策略 `0.9629324935` 提升 `0.0005133155`。P18 的可观测证据规则扩展到 24 个
视频，恢复 100 个事件；逐视频对照中，`val_014` 和 `val_023` 分别新增 3、6 个真阳性目标窗，
与高密度快速运动误差相符。几何参数邻域扫描中，`spatial_radius=5`、`min_track_bins=4`、
`max_link_distance=8` 仍为最优；普通 P0 的事件数/持续时间改动均无收益或明显退化。

相关日志：`log/analysis/m26_p18_all_full_verify.log`。

补充扫描：在 P18-global 已取消事件数上限的条件下，复用 M26 + M10 路由原始分数，对
`candidate_floor=0.49–0.57` 做了完整后处理重放。`0.53` 仍是唯一最优的平衡点：降至 `0.49–0.52`
时 Pd 不变但 Fa 上升，分数降至 `0.9632546281–0.9633895676`；升至 `0.54–0.55` 时 Fa 略降却丢失一个
目标窗，最佳仅为 `0.9634135881`；继续升高则 Pd 与 Score 均明显下降。该结果支持保留当前参数，
而不是针对验证集继续微调 P18。

相关日志：`log/analysis/m26_p18global_floor_scan.log`。

## M27：二步轨迹合成 flow 监督（不采用）

动机：M26 只在相邻时间箱监督 flow 的逆位移。对于快速目标，两个相邻 flow 组合后的累计位移仍无
直接约束。M27 在保持 M26 网络结构和推理完全不变的前提下，从当前时间箱的目标质心开始，连续采样
两个预测 flow，并以 Huber 损失对齐到两箱前的同一 `target_id` 质心。该监督只使用训练标注；推理时
仍只依赖事件流和 checkpoint。

实现：`utils/temporal_frame_loss.py` 新增 `target_centroid_trajectory_flow_loss`；
`train_temporal_memory.py` 新增可关闭的 M27 损失、日志和 checkpoint 元数据；两个 YAML 配置默认关闭。
M26 checkpoint 可直接加载，因为 M27 没有增加模型参数。合成三箱位移检查得到非零损失和梯度；真实
训练数据的一轮缩小采样冒烟检查处理 99 个序列、累计 1,750 条有效二步目标链，`flow_2` 反向传播正常。

正式训练从 M26 epoch 003 开始，保持 dense 8x、M26 的学习率和相邻 flow 监督，仅额外启用二步
损失，权重为 `0.15`。训练目录为
`log/m27_trajectoryflow_m26e3_dense8_e12_seed54/runs/20260811-113703_seed54_pid6960/`，共训练 12 个
epoch；最低训练 loss 为 epoch 011 对应的 `0.0401738439`，再次证明不能以训练 loss 选择 checkpoint。

所有 checkpoint 均使用固定的 M10 低密度路由、P6 `0.718 / 0.7226`、P0/P0c 和 P18-global（取消
事件数上限）进行完整 `test2.py` 验证：

| epoch | Score | Pd | IoU | Acc | Fa |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 001 | 0.9624236068 | 0.9769004620 | 0.9402877092 | 0.9747809172 | 4.7343951019e-06 |
| 003 | 0.9629831517 | 0.9781604368 | 0.9412474632 | 0.9765517712 | 4.8440150135e-06 |
| 004 | 0.9629921536 | 0.9783704326 | 0.9409064651 | 0.9763991237 | 4.8410523132e-06 |
| 008 | **0.9630374396** | 0.9783704326 | 0.9410371184 | 0.9765059948 | 4.8380896129e-06 |
| 011 | 0.9628776709 | 0.9783704326 | 0.9405248761 | 0.9760174751 | 4.8410523132e-06 |
| 012 | 0.9628804366 | 0.9783704326 | 0.9405387044 | 0.9760174751 | 4.8410523132e-06 |

结论：不采用，也不继续增加训练轮次。最佳 epoch 008 比当前 M26 + P18-global 最佳
`0.9634458090` 低 `0.0004083694`；其 Pd 也低 `0.0002099958`，IoU 与 Fa 同时变差。epoch 008 后分数
回落至 epoch 011/012 的约 `0.96288`，没有“训练更久会恢复”的趋势。说明相邻 target-flow 已足以提供
可学习的位移尺度，额外二步累计约束在当前权重下没有转化为泛化召回，反而干扰了原有的时序记忆校准。

相关日志：`log/analysis/m27_epoch001_p18global_verify.log`、
`log/analysis/m27_epoch003_p18global_verify.log`、
`log/analysis/m27_epoch004_p18global_verify.log`、
`log/analysis/m27_epoch008_p18global_verify.log`、
`log/analysis/m27_epoch011_p18global_verify.log`、
`log/analysis/m27_epoch012_p18global_verify.log`。

## M28：P18-global 高速轨迹连接（不采用）

动机：M26 + P18-global 的逐目标窗归因显示，P18 已恢复的 37 个目标窗运动速度 p99 为
`7.565 px / 50 时间单位`，与当前 `max_link_distance=8` 一致；仍漏检的 102 个目标窗速度 p90/p95 为
`12.072 / 14.574`，表明固定短链可能漏掉快速轨迹。

先在缓存上直接扫描固定连接距离 `8, 9, 10, 12, 14, 16, 18`。距离扩至 12 时 Pd 从
`0.9785804284` 升至 `0.9787904242`，但 Score 降至 `0.9633655842`；相对 8 像素，它仅新增 16 个恢复
事件，其中 1 个为真阳性、15 个为假阳性。因此不能直接放大连接距离。

实现了默认关闭的 M28 速度一致性门：常规 `<=8` 的 P18 恢复保持不变；仅对 `8–12` 的扩展链要求相邻
速度向量变化不超过设定上限。首次实现会让更宽贪心匹配重组原短链，已经修正为“标准 8 像素 P18
恢复与门控高速恢复的并集”，保证不会降低基线。对加速度上限 `0.5, 1.0, 1.5, 2.0, 3.0, 4.0, 6.0`
的缓存扫描全部精确回到 `0.9634458090`，没有接受任何可额外恢复的高速链。

结论：不采用。固定高速连接能找到一个真实目标，但该轨迹不满足稳定速度约束；在不使用标签的前提下，
继续放宽该规则会复现已量化的假阳性增长。M28 代码保留为默认关闭的诊断选项，不进入提交参数或长训练。

相关日志：`log/analysis/m26_p18global_target_attribution.log`、
`log/analysis/m26_p18global_link_scan.log`、`log/analysis/m28_p18global_velocity_scan.log`。

## M29：Pd 目标时间窗覆盖损失（不采用）

动机：当前剩余漏检中存在原始峰值接近决策阈值的目标窗。`train_temporal_memory.py` 已有未用于
M20/M26 链路的 P4 类训练期辅助项：按官方 `(target_id, 50 时间窗)` 分组，以平滑 noisy-OR 只要求每组
至少一个正事件越过置信度边界，理论上比抬高所有正事件更贴近 Pd。

先做一轮保守冒烟训练，而不是直接长训：从 M26 epoch 003 初始化，保持 dense 8x、attention、相邻
target-flow 和学习率倍率；仅启用 `metric_aux`，`target_weight=0.01`、`component_weight=0`、
`warmup=0`、激活阈值 `0.72`。训练中该损失平均为 `0.15516`，证明它确实参与梯度；target-flow 仍有
8,934 条有效相邻链。

完整 P18-global 验证：

```text
checkpoint: log/m29_targetcoverage_m26e3_smoke_seed55/runs/20260811-163728_seed55_pid8853/epoch_001_seed55.pt
IoU:      0.9412596226
Acc:      0.9757884741
Pd:       0.9769004620
Fa:       4.7343951019e-06
Score:    0.9627187451
```

结论：不采用，也不增加轮次。它比当前最佳低 `0.0007270639`，Pd 下降 `0.0016799664`；P18 的可恢复
事件也由 100 降至 94。说明该覆盖损失在已有强时序记忆模型上重排了高分证据，却没有把它转化为稳定的
泛化召回，继续训练的风险大于收益。

相关日志：`log/analysis/m29_targetcoverage_smoke_train.log`、
`log/analysis/m29_epoch001_p18global_verify.log`。

## M30：同架构高密度专家（2026-08-11，不采用）

动机：M26 + P18-global 的高事件密度视频仍是可观测的主要短板。M30 保持 M26 的
`width=16 + attention + target-flow` 架构，只在 `event_count > 100000` 的训练视频上继续
训练，并在推理时仅对同一可观测密度范围融合专家；低密度 M10 路由和中密度 M26 路由保持不变。

代码改动：`TemporalMemoryTrainDataset` 增加 dense-only 过滤和每轮固定轮换 3 个源视频；
训练入口增加 high-score background hard-negative；`test2.py` 和 `submit_challenge2.py` 增加
严格的 dense-specialist 路由。所有开关默认关闭。

初始化 checkpoint：`checkpoints/m26_targetflow_m20e3_epoch_003_seed53.pt`。

seed 56 的一轮冒烟只处理 15 个高密度源视频中的 3 个，共 6 个序列；hard-negative fraction
为非零，target-flow pair=221，训练无 NaN/Inf。基线完整复核仍为 `0.9634458090`。

| 专家权重 | Score | Pd | IoU | Acc | Fa |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 1.00 | 0.9634464805 | 0.9785804284 | 0.9416839480 | 0.9764296412 | 4.7669848054e-06 |
| 0.50 | 0.9634427639 | 0.9785804284 | 0.9416069388 | 0.9762922525 | 4.7580967044e-06 |
| 0.25 | 0.9634540048 | 0.9785804284 | 0.9416207671 | 0.9762922525 | 4.7551340041e-06 |

最佳的 `0.25` 仅比基线高 `0.0000081958`，低于 `+0.0001` 长训门槛。按决策树用 seed 57
复核同一权重：`Score=0.9634145106`、`Pd=0.9785804284`、`IoU=0.9414259195`、
`Acc=0.9760327339`、`Fa=4.7462459032e-06`，反而低于基线。因此这不是稳定增益，不进入
12/30 epoch 长训，也不更新公开最佳。

训练目录：`log/m30_dense_specialist_m26e3_smoke_seed56/`、
`log/m30_dense_specialist_m26e3_smoke_seed57/`。

## M31：训练期快速目标时间箱采样（2026-08-11，不采用）

动机：不再增加新损失，而是在训练期对同一 `target_id` 的相邻 50 时间单位质心位移进行预计算；
当位移至少为 4 像素时，将对应时间箱作为快速候选。只在 `event_count > 30000` 的额外 dense
视图中以 0.5 概率采样，基础视图保持旧逻辑，推理完全不使用标签。

代码改动：数据集新增可关闭的 motion sampling 参数和统计；训练 checkpoint 记录
`fast_bin_count` 与 `motion_selected_views`。M31 使用 M26 epoch 003 初始化，保持 dense 8x、
attention 和相邻 target-flow。

一轮训练共 408 个序列，`fast_bin_count=4311`、实际快速视图 `111`、target-flow pair=9032，
无 NaN/Inf。完整 P18-global 验证：

```text
checkpoint: log/m31_motion_sampling_smoke_seed56/runs/20260811-181627_seed56_pid10670/epoch_001_seed56.pt
IoU:      0.9405796528
Acc:      0.9750404358
Pd:       0.9769004620
Fa:       4.7284697013e-06
Score:    0.9625249021
```

结论：不采用，不增加轮数。尽管 Fa 略降，Pd 和 IoU 明显下降，较基线低 `0.0009209069`；说明
直接改变快速时间窗出现频率仍会破坏 M26 的已校准概率分布。

## M32：中密度额外视图 + 快速目标采样（2026-08-11，不采用）

动机：M31 失败后，按 M30 研究文档的后续分支，将 `30000 < event_count <= 200000` 的中密度视频
基础视图从 2 增至 4，同时保留 `>200000` 视频的 8 倍 dense views 和相同的快速目标采样，测试更平滑
的密度覆盖是否能改善 M31 的训练分布。

训练统计：39 个中密度视频新增 78 个视图；15 个高密度视频新增 210 个 dense views；共 486 个序列，
`fast_bin_count=4311`、实际快速视图 `143`、target-flow pair=10451，无 NaN/Inf。

```text
checkpoint: log/m32_mid_density_motion_smoke_seed56/runs/20260811-183156_seed56_pid10860/epoch_001_seed56.pt
IoU:      0.9405354261
Acc:      0.9749946594
Pd:       0.9766904662
Fa:       4.7255070010e-06
Score:    0.9624359585
```

结论：不采用，不增加轮数。比 M31 继续下降、比当前最佳低 `0.0010098505`；快速采样的退化不是中密度
视图不足导致，后续不应继续增加 motion sampling 概率、视图数或训练 epoch。

## M33：M26 与 M20 高密度融合（2026-08-11，不采用）

动机：M16/M17/M18 曾通过兼容时序模型融合获得小幅收益。M26 是从 M20 继续挂接 target-flow
得到的当前主模型，因此测试现有全局 blend 路径是否还能利用 M20 的互补误差。低密度 M10 路由、
P6 和 P18-global 全部固定，不按视频编号选择权重。

完整验证结果：

| M26 主权重 | M20 权重 | Score | Pd | IoU | Acc | Fa |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 0.90 | 0.10 | 0.9634277844 | 0.9785804284 | 0.9415159822 | 0.9761548638 | 4.7521713038e-06 |
| 0.98 | 0.02 | 0.9634458090 | 0.9785804284 | 0.9415289760 | 0.9761396050 | 4.7462459032e-06 |

为排除融合后的微小标定偏移，又对 90/10 只将高密度 P6 阈值从 `0.7226` 改为 `0.7220` 做完整验证，
结果为 `Score=0.9634271768`、`Pd=0.9785804284`、`IoU=0.9415324330`、
`Acc=0.9762006402`、`Fa=4.7551340041e-06`，仍低于基线。

结论：不采用融合。90/10 比基线低 `0.0000180246`；98/2 与基线逐项相同，没有真实增益。继续
扫描更细权重只会重复验证集离散阈值，不能证明未知测试集泛化，因此保留 M26 单模型。

## 后续记录模板

每次新实验完成后，按以下格式追加，避免只留下 checkpoint 路径：

```text
### Mxx：实验名称（日期）

动机：
代码改动：
初始化 checkpoint：
训练命令与关键超参数：
训练输出目录：
完整验证 checkpoint 与 Score/Pd/IoU/Acc/Fa：
后处理或融合扫描：
结论：采用 / 不采用，以及原因：
相关日志：
```

## M34：P0 抑制短轨迹的原始分数恢复（2026-08-11，不采用）

动机：现有 P18 必须依赖 P0/P0c 保留下来的高分 seed。假设部分真目标虽然形成短的
连续轨迹，但因持续时间不足被 P0 删除，导致 P18 无法恢复；若原始分数中仍有过阈值组件，
或许可用无标签的连续性规则谨慎恢复。

验证：基于固定的 M26+M10 原始分数缓存，仅对完整基线没有任何正预测的轨迹进行候选。
候选固定使用 P18 的 `candidate_floor=0.53`、空间半径 `5`、连接距离 `8`；扫描
轨迹长度 `4/5/6`、原始过阈值组件数量 `1/2`、以及每组件恢复或每轨迹仅恢复一个事件。
完整官方验证指标由缓存回放计算，标签仅由评估器读取。

结果：最保守的每轨迹恢复一个事件版本仍退化：`t=5, high=1` 新增 41 个事件、0 个真阳性，
`Score=0.9629848933`。每组件恢复的所有配置只新增 1 个真阳性，却增加 151--422 个事件，
Fa 和 IoU 均明显恶化；最佳仍是未改动的基线 `0.9634458090`。

结论：不采用，也不再扩大 P0/P18 的恢复范围。P0 删除的短连续高分轨迹在当前分布中主要是
背景噪声，继续做后处理只会针对验证集寻找偶然真阳性，不具有未知测试集泛化基础。

相关脚本：`log/analysis/sweep_m34_raw_track_recovery.py`。

## M35：H/4 细粒度运动对齐记忆（2026-08-11，不采用）

动机：M26 的 H/8 bottleneck flow 虽带来小幅增益，但离线诊断显示其预测输入位移中位数仅
`0.386 px/50`；对真实位移至少 `8 px/50` 的目标，方向一致率只有约 40%，不能安全用于
P18 高速连接。根因是常见目标位移 p90 约为 `4 px/50`，在 H/8 特征图仅为半个网格。

可行性诊断：对 M26 特征图进行相邻标注目标的局部余弦匹配（标签只用于离线测量）：

```text
H/4：发生网格级位移的样本 n=2417，top-1=22.1%，top-3=74.0%，mean rank=2.18
H/8：发生网格级位移的样本 n=1352，top-1= 7.8%，top-3=67.3%，mean rank=2.91
```

H/4 的 top-1 约为 H/8 的三倍，且 4px 约对应 H/4 的一个网格。因此 M35 在 `level2`
（H/4、width*4 通道）新增双向 ConvGRU 和记忆状态 flow warp；flow 直接沿用训练集
`target_id` 相邻质心的逆位移 Huber 监督，最大位移为 4 个 H/4 网格（约 16 输入像素）。
不加入 M25 已失败的全图 feature-consistency loss。

安全性与实现：细粒度 flow head 的末层和 H/4 残差投影均为零初始化。M26 作为初始化时，
M35 分支的残差严格为零；CPU 单元测试验证逐元素输出一致且 fine flow 收到非零目标监督梯度。
临时生成的零分支 M35 checkpoint 已完成完整 24 视频端到端回归，指标逐项等于当前最佳：

```text
IoU=0.9415289760  Acc=0.9761396050  Pd=0.9785804284
Fa=4.7462459032e-06  Score=0.9634458090
```

M35 端到端验证耗时约 151s（M26 约 136s），未出现显存异常。随后从 M26 epoch 003 初始化，
保持 dense 8x、attention、H/8 target-flow，以 `fine_memory_lr_multiplier=1.0`、其余 M26
分支 `0.05x` 训练 1 个完整 epoch。该 epoch 约耗时 1 小时，因此没有在无证据前安排多轮训练。

`best_loss_seed58.pt` 和 `last_seed58.pt` 用固定 M10 路由 + P6 + P0/P0c + P18-global 分别完整
验证，指标完全相同：

```text
IoU=0.9412475228  Acc=0.9758189917  Pd=0.9769004620
Fa=4.7403205025e-06  Score=0.9627024232
```

相对基线 Score 下降 `0.0007433858`，主要是 Pd 下降 `0.0016799664`；Fa 的极小改善不足以弥补。
结论：不训练第 2 轮，也不再继续 H/4 时序记忆。离线特征匹配更好不等于该高成本分支在当前数据上能
改善决策边界。

相关代码：`model/temporal_memory_net.py`、`train_temporal_memory.py`、
`utils/temporal_memory_inference.py`、`tests/test_m35_fine_temporal_memory.py`；
相关诊断：`log/analysis/analyze_m26_flow_guidance.py`、
`log/analysis/analyze_m35_fine_feature_matching.py`。

## 后处理与校准复查（2026-08-11，不采用）

为避免在新模型训练前遗漏低成本增益，基于固定的 M26+M10 原始分数缓存做了两项只依赖输入事件数的
回放。标签只由官方 evaluator 读取，不参与推理规则：

1. P18-global 的 `spatial_radius={2,3,4,5,6}`、`min_track_bins={3,4,5,6}`、
   `max_link_distance={7,8,9}` 共 60 组几何组合全部扫描。最优仍严格为
   `radius=5 / track=4 / link=8`，Score=`0.9634458090`。
2. 保持低密度 M10 的 `0.718` 与极高密度 M26 的 `0.7226` 不变，只把中密度
   `30000--100000` 的阈值从 `0.7196` 扫至 `0.7232`。最优为 `0.7220--0.7232`，均与当前
   两段 P6 规则逐项相同；较低阈值只增加 Fa/降低 IoU。

结论：当前与排行榜的差距不是 P18 几何或事件数阈值的可修补问题。继续放宽恢复会重演 M28/M34 的
假阳性增长；必须增强模型对真实目标和背景的可分性。

相关日志：`log/analysis/m26_p18global_geometry_scan.log`、
`log/analysis/m26_density3_mid_threshold_scan.log`。

## M36：冻结主干的零残差置信校准（2026-08-11，不采用）

动机：排行榜前列相对当前基线同时具有更低 Fa 和更高 IoU。M26 后处理及阈值已饱和，而 M29/M31/M35
表明继续更新主干会破坏已校准的 Pd。M36 仅在冻结 M26 的 decoder 特征上训练一个轻量置信分支，目标是
压低可泛化的背景高分，而不重训练时序记忆、attention 或 flow。

实现：原有 confidence head 改为**加性 logit residual**，末层权重和 bias 均初始化为零；推理使用
`sigmoid(base_logit + residual)`。因此把 M36 挂接到 M26 后，初始 logits、概率和完整 24 视频指标均
逐项等于基线。训练期的 class-balanced BCE 作用在 `base_logit.detach() + residual` 上，只有 confidence
head 接收梯度；主干、ConvGRU、attention、DACC 和 flow 参数全部冻结。

已完成检查：`tests/test_m36_residual_confidence.py` 通过；训练挂接检查确认仅存在 `confidence`
优化器组；零分支 checkpoint `log/analysis/m36_zero_attached_m26_e3.pt` 的完整验证精确复现
Score=`0.9634458090`。

首轮训练 1 epoch 后，`best_loss_seed60.pt` 与 `last_seed60.pt` 内容相同，完整验证也逐项一致：

```text
IoU=0.9406958818  Acc=0.9773150682  Pd=0.9783704326
Fa=5.0217770324e-06  Score=0.9625255455
```

相对 M26 + P18-global 基线，Score 下降 `0.0009202635`，IoU 下降 `0.0008330942`，
Pd 下降 `0.0002099958`，而 Fa 从 `4.7462459032e-06` 增至 `5.0217770324e-06`。这说明对所有
事件采用 class-balanced BCE 的轻量残差并未学到可泛化的背景抑制，反而改变了 P0/P18 所依赖的
高分种子分布。结论：不训练第 2 轮，也不再尝试同类全局 confidence residual；后续需保持 M26
概率校准，并从目标表征或训练样本覆盖入手。

相关日志：`log/analysis/m36_e1_bestloss_full_verify.log`、
`log/analysis/m36_e1_last_full_verify.log`。

## M37：完整时序记忆的水平镜像 TTA（2026-08-11，不采用）

动机：M36 失败后，不再通过新损失直接改变 M26 的概率分布。水平镜像是无需标签、对未知测试集同样
成立的几何变换；原流与镜像流保持同一事件顺序和时间箱，可在 P6/P0/P18 前逐事件平均分数。

实现：`utils/temporal_memory_inference.py` 新增完整事件流的 x 坐标镜像和分数融合；`test2.py`、
`submit_challenge2.py` 支持已有的 `INFERENCE_TTA.p14_horizontal_flip_*` 开关。默认关闭。CPU 单测
确认双重镜像能精确恢复坐标，且时间箱、标签和 target id 不变；M35/M36 既有单测也通过。

固定 M26 + M10 路由、P6、P0/P0c、P18-global 的完整验证：

| 原流权重 | Score | Pd | IoU | Acc | Fa |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 0.50 | 0.9487070587 | 0.9647207056 | 0.9097260833 | 0.9421121478 | 4.5477449821e-06 |
| 0.90 | 0.9623423789 | 0.9775304494 | 0.9387183785 | 0.9725521207 | 4.6632902944e-06 |

结论：不采用镜像 TTA，也不继续扫描更小权重。90/10 已比基线低 `0.0011034301`；50/50 的 Pd
大幅下降，说明当前模型对镜像事件流的预测不是可直接融合的互补误差。该开关保留为默认关闭的实验
基础设施，不能用于当前提交。

相关日志：`log/analysis/m37_hflip50_m26e3_full_verify.log`、
`log/analysis/m37_hflip90_m26e3_full_verify.log`。

## M38：训练期水平镜像增强（2026-08-11，不采用）

动机：M37 表明未增强的 M26 对镜像流不等变，但直接 TTA 混合会损伤 Pd。本实验只在训练期对输入
时序帧及其 event x 坐标同步镜像，推理仍只使用原始流；标签、目标 id、时间箱、事件密度和 M26
的相邻 target-flow 损失均保持原定义。

代码改动：`TemporalMemoryTrainDataset` 使用既有的 `SAMPLING.p15_*` 开关，按
`seed + epoch + video_index + view_index` 确定性决定翻转；镜像后同步反转时序帧宽度和 event x，
并在 checkpoint 中保存实际翻转视图数。默认关闭。M37 CPU 几何单测、M35/M36 回归测试通过。

从 M26 epoch 003 用原 M26 配方训练一轮：dense 8x、attention、相邻 target-flow、base/memory/
attention 为 `0.25x`、flow 为 `4x`。408 个训练序列中实际翻转 192 个，target-flow 有效 pair
累计 8,857，无 NaN/Inf。训练目录：
`log/m38_hflip_aug_m26e3_dense8_e1_seed61/runs/20260811-225023_seed61_pid2446/`。

完整验证（镜像 TTA 关闭，固定 M10 路由、P6、P0/P0c、P18-global）：

```text
IoU=0.9406627417  Acc=0.9750404358  Pd=0.9766904662
Fa=4.7106934994e-06  Score=0.9625083917
```

结论：不采用，也不增加 epoch。相对基线 Score 下降 `0.0009374173`、Pd 下降 `0.0018899622`、
IoU 下降 `0.0008662343`；其形态与 M29/M31/M35 一致，说明更新既有 M26 主干会破坏 epoch 003
的校准，后续新方向应冻结 M26 主干并仅训练零初始化的定向分支。

相关日志：`log/analysis/m38_hflip_aug_e1_train.log`、
`log/analysis/m38_hflip_aug_e1_full_verify.log`。

## M39：以冻结 M26 的 H/4 特征门控 P18 高速连接（2026-08-11，不采用）

动机：P18-global 使用 `max_link_distance=8` 时，能安全恢复一部分跨时间箱的低分目标；将距离放宽到
12 虽可额外恢复 16 个事件，但其中仅 1 个是真阳性、15 个是假阳性，不能直接放宽几何规则。M35 的
离线特征诊断曾显示 H/4 特征比 H/8 更具局部运动匹配能力，因此检查它能否作为仅依赖事件流与冻结模型
特征的门控信号。标签只用于离线报告，绝不参与推理规则或阈值选择。

实现：新增 `log/analysis/analyze_m39_p18_h4_affinity.py`，严格复刻 P18 在 `link=8` 和 `link=12`
下的轨迹构建，仅抽取后者新增恢复的事件；使用 M26 epoch 003 的原生输入尺度 `346x260`、
`whole_t=8000`，在 H/4 `level2` 上计算跨时间箱的直接余弦相似度及前一时间箱局部 7x7 搜索的最大相似度。

结果：16 个新增事件中，唯一真阳性的局部 affinity 为 `0.5395`；13 个具有有效相邻连接的假阳性
局部 affinity 范围为 `0.9176--0.9927`，中位数 `0.9771`。其余 2 个假阳性没有可计算的前驱连接。
这与预期相反：背景连续性在 H/4 特征上更相似，真目标的快速位移反而降低相似度。

结论：不采用，也不继续扫描 affinity 阈值或用其放宽 P18 高速连接。该信号不存在可泛化的真/假分离，
任何在验证集上选出的阈值都只是针对单个真阳性过拟合。相关日志：
`log/analysis/m39_p18_h4_affinity.log`。

## M40：冻结 M26 的 H/4 细粒度记忆分支（2026-08-11，不采用）

动机：M35 将 H/4 分支与 M26 的 encoder、原 ConvGRU、attention 和 H/8 flow 同时微调，即便旧模块
学习率仅为 `0.05x`，仍无法区分“新分支无效”和“主干校准被破坏”。M29/M31/M35/M38 的共同证据表明
M26 epoch 003 对主干更新敏感，因此 M40 仅训练 M35 新增且零初始化的 H/4 双向 ConvGRU、残差投影和
fine flow；M26 的全部参数及其运行统计保持冻结。

实现：

1. `train_temporal_memory.py` 新增默认关闭的
   `TEMPORAL_MEMORY.temporal_memory_fine_memory_only_enabled`。该模式只建立 `fine_memory` 优化器组、
   严格设置 `requires_grad`，并让主路径始终处于 `eval`，仅 H/4 四个新增模块处于 `train`。
2. 冻结模式不再对冻结的 H/8 flow 使用 target-flow 或两步轨迹监督；若误开会直接报出明确配置错误。
   H/8 的已保存架构仍保留，从而能严格加载 M26 checkpoint。
3. `model/temporal_memory_net.py` 修正了一个冻结模式专有问题：H/4 flow 的收集条件由顶层
   `model.training` 扩展为“顶层或 fine flow head 处于训练态”，否则 fine target-flow 损失会读到空序列。
4. `tests/test_m35_fine_temporal_memory.py` 增加冻结主路径时的 fine-flow 收集回归；
   `log/analysis/verify_m40_fine_only_training.py` 确认优化器参数和 `requires_grad` 掩码只包含 H/4 分支。
   M35/M36/M37 回归及 M40 挂接检查全部通过。

先用 4 个源视频做真实训练冒烟检查：22 个序列无 NaN/Inf，fine target-flow 有效 pair=`466`；完整
验证仍精确复现基线 Score=`0.9634458090`，确认冻结和加载链路没有改变 M26。随后按 M26 的 dense 8x、
单 epoch、原生 `346x260 / whole_t=8000` 完成两次完整独立训练；每次 408 个序列约 5 分 10 秒。
两个 seed 内的 `best_loss` 与 `last` checkpoint SHA-256 均相同，因此每个 seed 只需完整验证一次。

| seed | Score | Pd | IoU | Acc | Fa |
| ---: | ---: | ---: | ---: | ---: | ---: |
| M26 基线 | **0.9634458090** | 0.9785804284 | 0.9415289760 | 0.9761396050 | 4.7462459032e-06 |
| 62 | 0.9634233141 | 0.9785804284 | 0.9415012598 | 0.9761396050 | 4.7521713038e-06 |
| 63 | 0.9634256267 | 0.9785804284 | 0.9414857030 | 0.9761090875 | 4.7492086035e-06 |

结论：不采用，也不增加 epoch。两次独立训练均保持 Pd 不变却轻微降低 IoU、提高 Fa，分别比基线低
`0.0000224949` 和 `0.0000201823`。这说明冻结确实消除了 M35 的大幅 Pd 损伤，但 H/4 时序残差没有
产生足以改变最终检测的正向信息；继续增加轮次只会放大已经一致出现的轻微退化。相关日志：
`log/analysis/m40_fineonly_e1_train.log`、`log/analysis/m40_fineonly_e1_full_verify.log`、
`log/analysis/m40_fineonly_seed63_e1_train.log`、`log/analysis/m40_fineonly_seed63_e1_full_verify.log`、
`log/analysis/m40_fineonly_attachment.log`。

## P41：仅高密度 M26 路由的半时间箱相位集成（2026-08-12，采用）

动机：M36 的冻结置信残差训练仍破坏了 M26 的概率校准，而 M37 的水平镜像 TTA 也显著损伤 Pd；因此
后续只考虑不改变 checkpoint、且对未知测试集同样成立的输入重参数化。全事件流会以固定 50 时间单位
构成时序帧，目标恰好跨越分箱边界时，同一轨迹的上下文可能被切分。P41 将事件时间统一加 25，再按
同样的 50 时间单位重新分箱；原事件顺序和坐标不变，故两个流的逐事件分数可以在 P6/P0/P18 前直接
平均。这是标准的 temporal shift / cycle-spinning 思路，不读取标签或视频编号。

实现：`utils/temporal_memory_inference.py` 的 `TemporalPhaseTTAConfig` 与
`predict_temporal_memory_scores_with_temporal_phase()` 负责构造半箱相位流和概率平均；
`test2.py`、`submit_challenge2.py` 新增 P41 的 `min_event_count` 路由。默认值为 `0`，保持所有旧命令
行为不变；设为 `30000` 时，只有既有 M26 路由的 `event_count > 30000` 视频执行第二次相位推理，
低密度 M10 路由保持原始事件流。三个 YAML 均声明了该默认字段。`tests/test_m41_temporal_phase_tta.py`
已覆盖事件顺序、标签保留、非法整箱偏移和非法负路由阈值；`py_compile` 通过。

先固定 M26 + M10、P6、P0/P0c、P18-global 的全部生产参数，仅测试全局原流权重：

| P41 范围 | 原流权重 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 全部 M10 + M26 路由 | 0.50 | 0.9633247130 | 0.9777404452 | 0.9421269894 | 0.9764143825 | 4.7225443007e-06 |
| 全部 M10 + M26 路由 | 0.75 | 0.9635294833 | 0.9777404452 | 0.9424071312 | 0.9764601588 | 4.6721783953e-06 |
| 全部 M10 + M26 路由 | 0.90 | 0.9633066481 | 0.9779504410 | 0.9417430162 | 0.9762464762 | 4.7255070010e-06 |
| **仅 M26，event_count > 30000** | **0.75** | **0.9638562171** | **0.9785804284** | **0.9423022866** | **0.9763227701** | **4.6632902944e-06** |
| 仅 M26，event_count > 30000 | 0.50 | 0.9634305651 | 0.9783704326 | 0.9417464137 | 0.9763075113 | 4.7432832029e-06 |

结论：采用 `offset=25`、`original_weight=0.75`、`min_event_count=30000`。相对旧最佳
M26 + P18-global 的 `0.9634458090`，当前得分提高 `0.0004104081`；Pd 完全保持不变，IoU 提高
`0.0007733106`，Acc 提高 `0.0001831651`，Fa 从 `4.7462459032e-06` 降至 `4.6632902944e-06`。
全路由版本的 Pd 均下降，说明低密度 M10 对时间边界更敏感，不能一并集成；`30000` 是模型原有的公开
密度路由边界，未按视频或标签定制。50/50 高密度集成也未保住 Pd，因此不继续做偏移、权重或阈值的
细粒度扫描，避免对 24 视频验证集过拟合。

当前最佳生产推理新增配置：

```text
INFERENCE_TTA.p41_temporal_phase_enabled=true
INFERENCE_TTA.p41_temporal_phase_offset=25
INFERENCE_TTA.p41_temporal_phase_original_weight=0.75
INFERENCE_TTA.p41_temporal_phase_min_event_count=30000
```

相关完整日志：`log/analysis/m41_phase25_weight50_m26e3_full_verify.log`、
`log/analysis/m41_phase25_weight75_track4_m26e3_full_verify.log`、
`log/analysis/m41_phase25_weight90_track4_m26e3_full_verify.log`、
`log/analysis/m41_phase25_weight75_m26only_track4_full_verify.log`、
`log/analysis/m41_phase25_weight50_m26only_track4_full_verify.log`。

## M42：完整覆盖高密度专家 + hard-negative（2026-08-12，不采用）

动机：验证集仅有两个 `event_count > 100000` 高密度视频，但其误差同时包含大量 FP 和 FN；训练集则有
15 个同密度范围视频。因此只对该公开密度区间路由一个与 M26 隔离的专家，理论上能修复高密度统计分布，
且不会扰动已经稳定的 M10 低密度路由及常规 M26 视频。

与早期 M30 的 3 视频、2 view 冒烟不同，本轮从 M26 epoch 003 初始化，完整使用 15 个高密度训练视频、
每视频 6 个视图（共 90 个序列）、1 epoch、seed 64；保留 M26 的 advection/target-flow 训练配置，并启用
`hard_negative_weight=0.02`、`score_floor=0.50`。训练约 112 秒，权重为
`log/m42_dense_specialist_fullcover_e1_seed64/runs/20260812-005837_seed64_pid870/best_loss_seed64.pt`。

完整验证严格固定 M26 + M10 路由、P6、P0/P0c、P18-global 和 P41，只在 `event_count > 100000` 路由该专家，
融合权重 `0.25`：

| 配置 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前 M26 + P41 最佳 | **0.9638562171** | 0.9785804284 | **0.9423022866** | **0.9763227701** | 4.6632902944e-06 |
| M42 高密度专家，权重 0.25 | 0.9638077817 | 0.9785804284 | 0.9420778751 | 0.9760327339 | **4.6544021934e-06** |

结论：不采用，也不测试更高融合权重。专家仅使原始正例数从 `68018` 降至 `67983`，P0/P18 后从 `66320`
降至 `66297`；虽然 Fa 小幅下降，但 Pd 没有提升且 IoU/Acc 都下降，说明本轮 hard-negative 专家偏向保守
校准，无法解决高密度 FN。`0.25` 已呈反向趋势，增加专家权重只会放大这种抑制，属于无信息量的权重扫描。
后续若再做高密度专家，应移除 hard-negative，并采用只服务高密度目标召回的训练目标。完整日志：
`log/analysis/m42_dense_specialist_fullcover_e1_train.log`、
`log/analysis/m42_dense_specialist_w25_full_verify.log`。

## M43：高密度目标覆盖召回专家（2026-08-12，不采用）

M42 表明高密度 hard-negative 专家会过度压低已有分数，而高密度误差仍包含大量 FN。为避免 M29 直接
微调主模型时破坏全部密度区间，本轮仍只训练和路由 `event_count > 100000` 视频：从 M26 epoch 003
初始化，使用完整 15 个高密度训练视频、每视频 6 个视图、1 epoch、seed 65；关闭 hard-negative，开启
`metric_aux` 的目标时间窗覆盖项（`target_weight=0.01`、`component_weight=0`、`threshold=0.72`）。
训练末尾 `metric_pd=0.18872`，确认辅助项实际参与反向传播，且训练稳定无 NaN。

完整生产验证固定 M26 + M10、P6、P0/P0c、P18-global、P41，仅将专家以 0.25 权重作用于高密度视频：

| 配置 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前 M26 + P41 最佳 | **0.9638562171** | 0.9785804284 | **0.9423022866** | **0.9763227701** | 4.6632902944e-06 |
| M43 高密度召回专家，权重 0.25 | 0.9638457359 | 0.9785804284 | 0.9422261119 | 0.9762006402 | **4.6573648938e-06** |

结论：不采用，也不增加 epoch 或测试更高融合权重。虽然仅低于最佳 `0.0000104812`，但 Pd 仍完全不变，
原始正例数从 `68018` 降至 `68000`，P0/P18 后从 `66320` 降至 `66309`；它与 M42 一样在 0.25 权重时
就已表现为更保守的分数校准。继续训练或加大专家权重没有明确的正向证据，只会重复 M29/M30 的无效试错。
相关日志：`log/analysis/m43_dense_recall_specialist_e1_train.log`、
`log/analysis/m43_dense_recall_specialist_w25_full_verify.log`。

## P44：M26 路由的 2x2 空间相位集成（2026-08-12，不采用）

动机：P41 的半时间箱 cycle-spinning 能降低时间分箱边界误差；全卷积编码器存在下采样，理论上同样可能对
像素栅格相位敏感。因此将事件空间坐标循环平移 1 像素，形成原图、x、y、xy 四个相位；每路推理分数
均对应原事件顺序，四路等权平均后仍按原坐标执行 P6/P0/P18。实现默认关闭，且只对既有 M26 路由
（`event_count > 30000`）启用，M10 低密度模型不变。

实现新增 `SpatialPhaseTTAConfig` 和 `spatial_phase_shift_temporal_memory_video()`，并接入
`test2.py`、`submit_challenge2.py`。循环移位不会丢弃边界事件；P44 CPU 几何检查、既有 P41 检查及
`py_compile` 均通过。完整验证固定 M26 + M10、P41、P6、P0/P0c、P18-global：

| 配置 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前 M26 + P41 最佳 | **0.9638562171** | **0.9785804284** | **0.9423022866** | **0.9763227701** | 4.6632902944e-06 |
| P44 2x2 空间相位集成 | 0.9612298210 | 0.9766904662 | 0.9357324839 | 0.9693157673 | **4.6129243890e-06** |

结论：不采用，不扫描空间偏移或权重。P44 使原始正例从 `68018` 降至 `67433`，P0/P18 后从 `66320`
降至 `65847`；尽管 Fa 下降，IoU 和 Pd 均显著变差。这说明空间坐标循环移位产生的边界/位置分布变化
远大于可能的卷积相位收益，不能从 P41 的时间分箱成功外推到空间域。完整日志：
`log/analysis/m44_spatial_phase2x2_m26only_full_verify.log`。

## P45：P41 的 logit 空间融合（2026-08-12，不采用）

动机：P41 当前在概率空间以 `0.75 / 0.25` 平均原始与半时间箱相位分数。将两个时间量化视图视为独立
证据时，logit 平均比概率平均更能保留双视图一致的高置信度，可能减少平均后恰好跌过阈值的真目标。
本轮严格保持 P41 的 `offset=25`、权重、`event_count > 30000` 路由以及全部生产模型/后处理不变，只改
融合域；默认仍为原有 `probability`，因此所有旧命令结果不变。

实现：`TemporalPhaseTTAConfig` 新增 `average_mode`（`probability` 或 `logit`），
`blend_temporal_phase_scores()` 对概率做数值安全的 log-odds 平均。CPU 测试验证 0.9/0.5 在
`0.75 / 0.25` 下由概率平均的 0.8 变为 logit 平均的 0.83861，且非法模式会拒绝；P41/P44 几何测试和
`py_compile` 均通过。

| 配置 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: |
| P41 概率平均（当前最佳） | **0.9638562171** | **0.9785804284** | **0.9423022866** | 0.9763227701 | 4.6632902944e-06 |
| P45 logit 平均 | 0.9636440827 | 0.9785804284 | 0.9420565367 | **0.9766433835** | 4.7314324016e-06 |

结论：不采用，不再扫描融合域。logit 平均让原始正例从 `68018` 增至 `68102`，但 P0/P18 后也从
`66320` 增至 `66381`；Pd 没有改善，Fa 明显上升并拉低总分。原始概率平均的保守性正是 P41 泛化收益的
关键，应保持 `p41_temporal_phase_average_mode=probability`。完整日志：
`log/analysis/m45_phase_logit_m26only_full_verify.log`。

## M46：25 时间单位独立时序记忆专家（待训练）

动机：P41 的增益来自时间分箱边界，而 P44 空间相位和 P45 融合域均明确失败；说明下一步不应继续做
分数校准或空间变换，而应让专家直接观察更细的时间帧。M46 使用 25 时间单位而非 M26 的 50，训练序列
长度由 16 增至 32，从而保持约 800 时间单位的训练时域；推理时完整视频仍使用双向记忆。它只路由到
`event_count > 30000`，恰好覆盖 M26 的 14 个验证视频及训练集对应的 54 个视频，M10 低密度路由不受影响。

实现与安全检查：`TemporalMemoryInferenceConfig` 增加默认关闭的 fine-time expert 路由；`test2.py` 与
`submit_challenge2.py` 仅在路由命中时构造第二套 25-bin 事件帧并融合分数。`train_temporal_memory.py`
默认拒绝不同时间长度/分箱的初始化，M46 必须显式打开两个迁移开关；这是安全的，因为 ConvGRU、attention
和 flow 参数形状不依赖序列长度，且输入通道仍为 5 个时间箱 x 2 polarity。M46 配置测试、P41/P44 回归、
`py_compile`，以及真实 M26 checkpoint 到 `bin=25 / sequence=32` 的严格 state_dict 加载均通过。

首轮训练固定从 M26 epoch 003 初始化，使用 54 个 `>30000` 训练视频、每视频 6 view、3 epoch；保留
M26 的 attention、advection 和 target-flow，不叠加 M29 覆盖损失、hard-negative、空间或时间 TTA。
训练预计超过一小时，交由用户运行。训练结束后依次以 epoch 001/002/003 为 fine-time expert，固定当前
M26 + M10 + P41 + P6/P0/P0c/P18-global，只测试预先固定的专家权重 0.25；仅当有整体增益才测试 0.50。

### M46 短冒烟结论（2026-08-12，不采用）

原计划的 54 视频 x 6 view x 3 epoch 在真实设备上每个 `bin=25 / sequence=32` 序列约 21.9 秒，单 epoch
约两小时，不符合“分钟级单轮、多 checkpoint 筛选”的已验证有效工作流。因此不继续该长训，仅以 3 个
轮换视频、每视频 2 view、共 6 序列做一次实际冒烟（seed 67）。训练稳定，M26 到细时间模型的初始化、
advection/target-flow 梯度和 checkpoint 保存均正常，耗时 2 分 11 秒。

将这个 checkpoint 仅以预先固定的 0.25 权重路由到 `event_count > 30000`，同时保持 M26 + M10、P41、
P6、P0/P0c、P18-global 不变，完整验证结果为：

| 配置 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前 M26 + P41 最佳 | **0.9638562171** | **0.9785804284** | **0.9423022866** | **0.9763227701** | 4.6632902944e-06 |
| M46 25-bin 短专家，权重 0.25 | 0.9629786769 | 0.9750104998 | 0.9415542483 | 0.9736360312 | **4.3255424586e-06** |

结论：不采用，不跑长训。它使原始正例从 `68018` 降至 `67499`，P0/P18 后从 `66320` 降至 `66011`；Fa
虽降低，但 Pd、IoU、Acc 均明显变差。细时间分辨率不是可直接从 M26 迁移的互补专家，且训练成本与此前
成功的分钟级多 epoch 试验不匹配。相关日志：
`log/analysis/m46_fine_t25_s32_smoke3_e1_seed67_train.log`、
`log/analysis/m46_fine_t25_s32_smoke3_w25_full_verify.log`。

## M47：P41 半时间箱相位单视图诊断（2026-08-12，不采用训练）

动机：P41 在 M26 路由上以原流 `0.75`、半时间箱相位流 `0.25` 的概率平均将当前最佳提升至
`0.9638562171`。在考虑任何“相位专用模型”之前，先检查相位流本身是否是一个足够稳定的独立视图，
避免把 P41 的保守平滑效果误判为需要训练 `25-bin / sequence=32` 专家的证据。

验证：保持 M10 低密度路由、M26 epoch 003、P6、P0/P0c 与 P18-global 完全不变；仅对
`event_count > 30000` 视频启用 P41，固定 `offset=25`，并将原流权重固定为 `0.0`，即只使用
半时间箱重新分箱后的 M26 分数。此诊断不训练、不修改 checkpoint。

```text
IoU=0.9384710193  Acc=0.9746740460  Pd=0.9787904242
Fa=5.0040008305e-06  Score=0.9620351886
```

相对当前 P41 最佳，单相位流只增加了一个 Pd 目标窗，却损失了 `0.0038312673` IoU、
`0.0016487241` Acc，且 Fa 从 `4.6632902944e-06` 升至 `5.0040008305e-06`。因此 P41 的
泛化价值来自原流为主的概率平均，而非相位流可以独立替代原流；不能以此为理由继续 M46 的
`25-bin / sequence=32` 长训，也不应做相位权重、偏移或阈值扫描。

结论：**M46 不采用，不跑长训。** 下一候选如要利用 P41，应保持原始 `50-bin / sequence=16`
的分钟级成本：仅训练一个隔离的半相位 M26 专家，并且只在 `event_count > 30000` 的 P41 路由内以
预先固定的 `0.25` 权重参与原 M26 分数融合。它必须先完成零步骤等价检查和 1 epoch 冒烟；只有
Score 至少不低于当前最佳 `0.9638562171` 且 Pd 不下降，才值得按 M13/M15/M20/M26 的模式训练
12--30 epoch 并逐轮验证。若冒烟退化，则整个相位训练方向停止，不再进行参数扫描。

相关日志：`log/analysis/m47_phaseonly_m26e3_full_verify.log`。

## M48：目标中心时序记忆（2026-08-12，不采用）

动机：P41 之后继续修改全局置信度或空间域已有明确退化证据，因此尝试一个只观察目标中心热图的
窄时序分支。M48 新增目标中心热图头、低分辨率双向 ConvGRU 中心记忆，以及中心残差到事件 logit
的零初始化投影；M26 主干、原 ConvGRU、attention、flow 和 DACC 全部冻结，只训练新增分支。
默认开关关闭，零分支严格保持 M26 输出不变。

训练与验证：从当前 M26 epoch 003 初始化，使用高密度 `dense_view_multiplier=8`，每个 seed 训练 1
epoch；seed 69 与 seed 70 均通过零扰动、梯度、仅新增参数进入优化器、`py_compile` 及 M35/M36/M41
回归检查。完整验证固定 M10 低密度路由、M26/P41、P6、P0/P0c、P18-global：

| 配置 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前 M26 + P41 最佳 | **0.9638562171** | **0.9785804284** | **0.9423022866** | **0.9763227701** | 4.6632902944e-06 |
| M48 seed 69 | 0.9638517467 | 0.9785804284 | 0.9422875643 | 0.9763075113 | 4.6632902944e-06 |
| M48 seed 70 | 0.9638517467 | 0.9785804284 | 0.9422875643 | 0.9763075113 | 4.6632902944e-06 |

两个独立 seed 完全一致且均低于当前最佳 `0.0000044704`；Pd、Fa 不变，IoU/Acc 同步下降，说明
中心记忆没有提供可改变最终检测决策的泛化证据。结论：**M48 不采用，不增加 epoch、不扫描融合权重或
阈值。** 相关日志：`log/analysis/m48_zero_attached_full_verify.log`、
`log/analysis/m48_center_memory_e1_full_verify.log`、
`log/analysis/m48_center_memory_seed70_e1_train.log`、
`log/analysis/m48_center_memory_seed70_e1_full_verify.log`。

## M49：P41 半时间箱相位专用专家（短冒烟待验证）

动机：P41 的唯一稳定收益来自 50 时间单位分箱的边界误差，而 M47 证明相位流单独替代原流会退化，
M46 证明 25 时间单位重建模型成本过高且迁移不匹配。M49 保留 M26 的原始 `50-bin / sequence=16`
架构和概率校准，仅让一个同架构专家在 P41 的半箱偏移视图上训练，再替换 P41 原本未经训练的第二
视图。推理严格使用固定 `75% M26 原流 + 25% M49 相位专家`，只对 `event_count > 30000` 路由；低密度
M10、中密度 M26、P6、P0/P0c、P18-global 均不变。不得与 M30/M42/M43 专家、M46 细时间专家、P44
空间相位或额外 blend 叠加。

实现：`dataset/temporal_frame.py` 增加相位重建函数，`dataset/temporal_memory.py` 增加默认关闭的
`temporal_phase_offset` 训练视图，`test2.py` 与 `submit_challenge2.py` 增加 M49 专家路由；
`utils/temporal_memory_inference.py` 和三个 YAML 增加配置及一致性校验。M49 关闭时旧命令完全不变。
CPU 检查、P41 回归、M49 结构检查和 `py_compile` 均已通过；用 M26 checkpoint 作为零步专家的 24 视频
等价验证严格得到当前最佳 `0.9638562171`。

短冒烟决策标准：M49-e1 完整验证 Score 必须至少达到 `0.9638562171` 且 Pd 不下降，才考虑继续
12--30 epoch；若低于最佳 `0.0001` 或 Pd 下降，立即否决，不扫描权重。由于一轮约数分钟，短训练和
完整验证由执行模型直接完成；若后续长训预计超过 20 分钟，再把 WSL 指令交由用户运行。

### M49-e1 双 seed 结果（2026-08-12）

两次独立 1 epoch 训练均完成了 324 个相位视图，target-flow 配对非零且无 NaN/Inf。完整验证均固定
当前 M10/M26 路由、P41 概率融合、P6、P0/P0c 与 P18-global；M49 专家权重固定为 0.25：

| 配置 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前 M26 + P41 最佳 | **0.9638562171** | **0.9785804284** | **0.9423022866** | **0.9763227701** | 4.6632902944e-06 |
| M49-e1 seed72 | **0.9638883076** | **0.9785804284** | 0.9423431158 | 0.9763075113 | 4.6544021934e-06 |
| M49-e1 seed73 | **0.9638725759** | **0.9785804284** | 0.9423145056 | 0.9762922525 | 4.6573648938e-06 |

seed72 相对当前最佳提升 `0.0000320905`，seed73 提升 `0.0000163588`；两次 Pd 均不下降，Fa 均略降，
说明相位专用训练确实在当前验证集产生了稳定但很小的互补收益。由于尚未达到 `+0.0001` 的长训门槛，
不立即进行 12--30 轮；但该方向不同于 M46 的细时间迁移，且两 seed 同向，因此安排一次有限的 6 epoch
延长训练。训练约 6 分 40 秒/epoch，预计总计约 40 分钟，必须由用户运行；完成后只完整验证 epoch
`001--006`，若最高分不超过 `0.9638883076` 且不能在独立 seed 复现，则否决 M49，不改生产方案。

### M49-e6 延长训练结果（2026-08-12，否决）

用户从 `M49-e1 seed72 epoch_001` 继续训练 6 个 epoch，配置保持相位视图、dense-only 路由、
target-flow、attention、学习率和 `75% M26 原流 + 25% M49 相位专家` 推理融合不变。训练目录为
`log/m49_phase_specialist_m26e3_e6_seed72/runs/20260812-152524_seed72_pid3798/`；训练 loss 在第
5 轮最低（`0.0323255125`），但不能以 loss 选择 Challenge 2 checkpoint。

6 个 checkpoint 均使用完整 24 视频验证，固定 M10 低密度路由、M26 主模型、P41 概率融合、P6、
P0/P0c 和 P18-global。注意这里的 `epoch_001` 是在 M49-e1 之后的总第 2 轮，不是原来的 e1：

| 继续训练轮次 | IoU | Acc | Pd | Fa | Score |
| ---: | ---: | ---: | ---: | ---: | ---: |
| M49-e1 seed72（继续训练前） | 0.9423431158 | 0.9763075113 | **0.9785804284** | 4.6544021934e-06 | **0.9638883076** |
| e6 epoch 001（总第 2 轮） | 0.9421645999 | 0.9760937691 | 0.9769004620 | 4.6484767928e-06 | 0.9631762119 |
| e6 epoch 002（总第 3 轮） | 0.9421654344 | 0.9761090875 | 0.9769004620 | 4.6514394931e-06 | 0.9631694263 |
| e6 epoch 003（总第 4 轮） | 0.9421073794 | 0.9760632515 | 0.9769004620 | 4.6544021934e-06 | 0.9631447477 |
| e6 epoch 004（总第 5 轮） | 0.9421073794 | 0.9760632515 | 0.9769004620 | 4.6544021934e-06 | 0.9631447477 |
| e6 epoch 005（总第 6 轮） | 0.9421073794 | 0.9760632515 | 0.9769004620 | 4.6544021934e-06 | 0.9631447477 |
| e6 epoch 006（总第 7 轮） | 0.9420925975 | 0.9760479927 | 0.9769004620 | 4.6544021934e-06 | 0.9631402655 |

相对 M49-e1，继续训练第一步就损失 `0.0016799664` Pd 和 `0.0001785159` IoU；之后没有任何
反弹，最低分比当前正式 P41 基线 `0.9638562171` 低 `0.0007159516`。训练 loss 下降与最终分数
完全相反，说明相位专家对验证集的有用互补只存在于极早期的校准状态，继续更新会破坏 P41 所依赖的
概率分布。Fa 的小幅下降无法弥补 Pd/IoU 损失，不能通过调大专家权重挽救；也不扫描权重或阈值，
避免把 24 个公开验证视频过拟合成生产规则。

结论：**M49 不采用，不再延长训练，也不做 seed73 的长训或权重扫描。** 正式生产继续使用
P41 的 `M26 原流 + 未训练半时间箱相位流`（`Score=0.9638562171`）。M49-e1 的两个 seed
虽有 `+0.000016--+0.000032` 的小幅验证集增益，但没有达到预设的 `+0.0001` 门槛，且续训稳定
退化，不能作为未知测试集泛化证据。相关完整日志：
`log/analysis/m49_phase_specialist_e6_seed72_epoch001_full_verify.log` 至
`log/analysis/m49_phase_specialist_e6_seed72_epoch006_full_verify.log`。

## M50：高密度多相位无训练 TTA（2026-08-12，不采用）

动机：M49 继续训练会破坏相位专家的概率校准，M41 的收益又来自原始 50-bin 流与单个半箱相位流
之间的边界平滑。M50 在不改 checkpoint 的前提下，将高密度 M26 路由的相位视图扩展为三个偏移
`12、25、37`，保持原始流权重 `0.75`，三个相位流共同分配剩余 `0.25`；低密度 M10、中密度
M26、P6、P0/P0c、P18-global 均保持不变。该规则只依赖输入 `event_count > 30000`，因此具备
对未知测试集可执行的形式。

实现：`utils/temporal_memory_inference.py`、`test2.py`、`submit_challenge2.py` 和三个 YAML
加入默认关闭的多相位配置；关闭时旧命令逐项回归一致。CPU 检查、`py_compile`、M41/M49 回归
均通过。完整验证日志为 `log/analysis/m50_multiphase_12_25_37_m26only_full_verify.log`。

验证固定当前生产协议：M10 低密度路由、M26 epoch 003、P41/P50 只作用于 `event_count > 30000`、
原始权重 `0.75`、P6/P0/P0c/P18-global 全部固定。24 个公开验证视频结果如下：

| 配置 | IoU | Acc | Pd | Fa | Score |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前 P41 单相位最佳 | **0.9423022866** | **0.9763227701** | **0.9785804284** | **4.6632902944e-06** | **0.9638562171** |
| M50 三相位（12/25/37），原流 0.75 | 0.9415221214 | 0.9760174751 | 0.9771104578 | 4.7492086035e-06 | 0.9628357609 |

M50 相对当前最佳下降 `0.0010204562`，IoU、Acc、Pd、Fa 全部恶化；原始正例由 `68018` 降至
`67998`，P0/P18 后由 `66320` 降至 `66234`。这说明额外相位流不是互补证据，反而把单个
`offset=25` 带来的保守平滑稀释为多视图平均；不能通过继续训练、相位权重或偏移扫描挽救，避免
在 24 个公开视频上过拟合。结论：**M50 不采用，不继续训练，也不改变当前生产配置。**

截至本节，正式最佳仍为 M26 + M10 低密度路由 + P41 + P6/P0/P0c/P18-global，
`Score=0.9638562171`。后续候选必须先证明能改善 P41 保留下来的真实正例召回，同时不能破坏
M26 的概率校准；不再尝试无训练的额外相位、logit 融合或细时间重建。

## M52：冻结主干的快速目标 flow-only 再监督（2026-08-12，不采用）

动机：P41 逐目标归因表明，剩余原始低分漏检的相邻时间箱真实位移中位数为 `5.427 px/50`，而 M26
预测 flow 中位数仅为 `0.079 px`。为避免再次破坏 M26 epoch 003 的主概率校准，本轮冻结 base、双向
ConvGRU、attention 与 memory projection，只训练已有的 flow head；仅保留目标质心相邻 flow 监督，
取消 BCE、特征一致性与 M27 两步轨迹损失，位移至少 `4px` 的标注目标对赋予 4 倍权重。

训练目录：`log/m52_flowonly_fast4w4_m26e3_dense8_e1_seed74/`。1 epoch 共 408 个训练序列，耗时约
4 分钟，有效 flow 对 `8840`，其中快速对 `1421`；优化器参数检查确认只有 flow head 被更新。

完整验证固定 M10/M26 路由、P41、P6、P0/P0c 与 P18-global：

| 配置 | IoU | Acc | Pd | Fa | Score |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前正式最佳 | **0.9423022866** | **0.9763227701** | **0.9785804284** | **4.6632902944e-06** | **0.9638562171** |
| M52 flow-only e1 | 0.9423940778 | 0.9764754176 | 0.9783704326 | 4.6751410956e-06 | 0.9637719112 |

M52 虽提升了已检出目标的 flow 中位数（`0.518 -> 0.760 px`）和方向一致性，但剩余低分漏检的
flow 中位数仅由 `0.079 -> 0.118 px`，没有转化为 Pd；得分反而低 `0.0000843059`。结论：**不增加
epoch、不换 seed、不扫描快速权重或融合权重，也不再训练同一个全局 flow head。** 相关日志：
`log/analysis/m52_flowonly_fast4w4_full_verify.log`、
`log/analysis/m52_flowonly_fast4w4_attribution.log`。

## M53：P41 时间边界自适应融合（2026-08-12，不采用）

动机：P41 的半时间箱偏移只用于修复原始 `50` 时间单位分箱边界，因此验证一个不依赖标签、没有额外
超参数扫描的固定规则：原始时间箱边界处使用 `50%` 原流与 `50%` 相位流，向时间箱中心线性提升原流
权重到 `100%`。均匀时间覆盖下的原流平均权重严格为既有 P41 的 `75%`，仅把相位流质量集中到它理论上
最有用的位置。

CPU 几何单测确认权重范围为 `[0.5, 1.0]`、半箱偏移要求和均匀时间平均值 `0.75`；完整验证仍固定
M10/M26 路由、P6、P0/P0c、P18-global 与 P41 的其他参数：

| 配置 | IoU | Acc | Pd | Fa | Score |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前正式最佳 | **0.9423022866** | **0.9763227701** | **0.9785804284** | **4.6632902944e-06** | **0.9638562171** |
| M53 P41 边界自适应 | 0.9422119260 | 0.9764449000 | 0.9785804284 | 4.7077307991e-06 | 0.9637231391 |

Pd 没有改善，IoU 降低且 Fa 上升，得分低 `0.0001330780`。这表明 P41 的收益不是由“距单个原始
分箱边界的事件位置”即可局部定位，而是来自全局保守平均。结论：**不采用，不扫描线性曲线、边界宽度
或新的融合权重。** 相关日志：`log/analysis/m53_boundary_adaptive_p41_full_verify.log`。

## M54：高密度双向记忆的时间反转 TTA（2026-08-12，不采用）

动机：M26 的两个 ConvGRU 分别学习正向与反向的时序状态，参数并不共享。将完整事件流按
`t' = whole_t - 1 - t` 反转可交换这两个方向的作用，同时保持每个事件的原数组索引不变；因此可在
不训练、不读取标签、不改变 P6/P0/P18 的前提下得到一条方向互补视图。本轮仅在原有高密度 M26 路由
上测试预先固定的 `75%` 当前 P41 分数加 `25%` 时间反转 P41 分数，低密度 M10 完全不受影响。

CPU 检查确认反转两次可逐元素恢复原 `x/y/t`、标签和 target id，且反向分箱索引正确；P41 回归和
语法检查通过。完整验证结果：

| 配置 | IoU | Acc | Pd | Fa | Score |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前正式最佳 | **0.9423022866** | **0.9763227701** | **0.9785804284** | 4.6632902944e-06 | **0.9638562171** |
| M54 时间反转 + P41 | 0.9411123395 | 0.9749030471 | 0.9750104998 | 4.6662529947e-06 | 0.9620398009 |

得分下降 `0.0018164162`，Pd 与 IoU 均显著退化。说明双向状态已在原方向上形成非对称但有用的校准，
反向时间视图不是可安全平均的互补误差。结论：**不采用，不扫描反转权重，也不再尝试同类时间方向 TTA。**
相关日志：`log/analysis/m54_temporal_reverse_p41_full_verify.log`。

## M55：从 M20 重训 M26 的独立种子复核（2026-08-12，不采用）

动机：M26 的正式 checkpoint 来自 M20 epoch 003 的单次训练链。为区分它是否只是偶然的
seed 结果，按原 M26 配方从同一 M20 起点重新训练独立 seed 75：dense 8x、attention、相邻
target-flow、base/memory/attention 学习率 `0.25x`、flow 学习率 `4x`。此实验不改 P41、P6、
P0/P0c 或 P18-global。

训练 3 个 epoch 后，只验证了前两轮，因为分数已不具备继续筛选价值：

| checkpoint | IoU | Acc | Pd | Fa | Score |
| --- | ---: | ---: | ---: | ---: | ---: |
| P41 正式基线 | **0.9423022866** | 0.9763227701 | **0.9785804284** | 4.6632902944e-06 | **0.9638562171** |
| M55 epoch 001 | **0.9424844980** | 0.9763532877 | 0.9781604368 | **4.6455140925e-06** | 0.9637786178 |
| M55 epoch 002 | 0.9423261285 | **0.9767501950** | 0.9783704326 | 4.7225443007e-06 | 0.9636501171 |

再以固定 `M26:0.90 + M55-e1:0.10` 仅融合高密度 M26 路由，Score=`0.9637654404`，仍低于基线。
结论：**不采用，不验证 epoch 003，也不再尝试同配方重训或其权重融合。** M26 epoch 003 的有效
校准不能由独立重训稳定复得，继续搜索重训 seed 属于验证集选优而非可泛化改进。

相关日志：`log/analysis/m55_m26_reseed_e1_seed75_full_verify.log`、
`log/analysis/m55_m26_reseed_e2_seed75_full_verify.log`、
`log/analysis/m57_m26_m55e1_blend90_p41_full_verify.log`。

## M56：剩余漏检与 M5 轨迹外推先验的适配性诊断（2026-08-12，否决）

动机：P41 后仍有低分目标窗，而早期 M5 曾带来显著增益；在重新启用该 loss 前，先用标签仅作离线
测量，检查其线性外推假设是否覆盖当前漏检。M5 的 loss 只监督序列中缺少标注的时间箱，不能直接
监督已标注但低分的目标窗。

直接覆盖诊断显示 `4765` 个正目标时间箱中，M5 可直接监督的数量为 `0`（`0.00%`）。留一时间箱
质心线性拟合进一步显示，`102` 个 raw-low-score 目标窗的误差中位数为 `2.30 px`、p90 为
`10.66 px`、均值为 `4.30 px`；正常检出窗的误差中位数仅 `0.32 px`、p90 为 `1.14 px`。

结论：**不重新启用或调参 M5。** 该先验既不直接覆盖剩余漏检，也不满足它们所需的线性运动条件；
重新训练只会把不适用的约束施加给整个模型。

相关日志：`log/analysis/m56_m5_direct_coverage.log`、`log/analysis/m56_m5_linearity.log`。

## M58：M49 相位专家第三独立 seed（2026-08-12，保留为候选，不延长训练）

动机：M49 的 seed 72 与 73 都在单轮、固定 `0.25` 相位专家融合下小幅超过 P41，且 Pd 未下降；
但 seed 72 的第 2 个训练 epoch 起立即明显退化。因此只做第三次独立单轮复核，而不再扫描学习率、
融合权重或训练轮数。

M58 从 M26 epoch 003 初始化，固定 `event_count > 30000` 的 54 个训练视频、每视频 6 个相位
视图、50-unit bin、offset 25、attention、相邻 target-flow 与 M49 原学习率。324 个序列训练正常完成，
耗时约 6 分 45 秒；完整验证固定 M10/M26 路由、P41、P6、P0/P0c 和 P18-global：

| 配置 | IoU | Acc | Pd | Fa | Score |
| --- | ---: | ---: | ---: | ---: | ---: |
| P41 正式基线 | 0.9423022866 | **0.9763227701** | **0.9785804284** | 4.6632902944e-06 | 0.9638562171 |
| M49-e1 seed72 | **0.9423431158** | 0.9763075113 | **0.9785804284** | **4.6544021934e-06** | **0.9638883076** |
| M49-e1 seed73 | 0.9423145056 | 0.9762922525 | **0.9785804284** | 4.6573648938e-06 | 0.9638725759 |
| M58-e1 seed76 | 0.9422997832 | 0.9762769938 | **0.9785804284** | 4.6573648938e-06 | 0.9638681055 |

三次独立训练均高于 P41，平均增益约 `0.0000208`，且三次 Pd 都没有下降；因此“只训练一轮的
半相位专家”具有重复的弱正信号。另一方面，2-seed 权重平均仅为 `0.9638770462`，低于 seed72 单模型，
而 seed72 继续训练第 2 轮即降至 `0.9631762119`。这些结果不足以证明它会在未知测试集稳定胜过
P41，更不足以支撑更长训练或超参数扫描。

结论：**M49/M58 只保留为候选提交版本，不纳入当前正式基线，也不继续训练。** 后续只有在独立、
不依赖 M49 的改进方向取得明确增益后，才可重新比较二者是否互补；禁止将其作为继续堆叠专家的理由。

相关日志：`log/analysis/m58_phase_specialist_e1_seed76_train.log`、
`log/analysis/m58_phase_specialist_e1_seed76_full_verify.log`。

## M59：历史 M10 checkpoint 在当前协议下的回溯复核（2026-08-12，不采用）

动机：M51 归因中，剩余 `102` 个原始低分目标窗有 `45` 个来自低密度 M10 路由；M10 原训练共保存
e1--e4，而生产只固定 e2。在引入任何新低密度专家前，先以当前完整协议复核已有 checkpoint，避免
重复训练一个已知会退化的原路径。P41 仍仅在 M26 路由启用，因此三个实验之间的差异完全来自 M10。

| M10 checkpoint | IoU | Acc | Pd | Fa | Score |
| --- | ---: | ---: | ---: | ---: | ---: |
| M10 e2（生产基线） | **0.9423022866** | **0.9763227701** | **0.9785804284** | 4.6632902944e-06 | **0.9638562171** |
| M10 e1 | 0.9419723749 | 0.9756358266 | 0.9741705166 | 4.6336632912e-06 | 0.9620424196 |
| M10 e3 | 0.9415625334 | 0.9745213985 | 0.9731205376 | **4.5270060799e-06** | 0.9617346633 |
| M10 e4 | 0.9415963888 | 0.9746282697 | 0.9733305334 | 4.5388568812e-06 | 0.9618021430 |

结论：**继续固定 M10 e2，不从其原训练轨迹继续、回退或扫描 checkpoint。** e1 已损失约 21 个
Pd 目标窗，e3/e4 损失更多；它们较低的 Fa 无法弥补召回和 IoU 的下降。后续低密度方向若存在，必须
使用与 M10 原路径不同、并且只依赖输入可观测量的机制。

相关日志：`log/analysis/m59_m10_e001_with_m26p41_full_verify.log`、
`log/analysis/m59_m10_e003_with_m26p41_full_verify.log`、
`log/analysis/m59_m10_e004_with_m26p41_full_verify.log`。

## M60：取消 M10 低密度路由的消融（2026-08-12，不采用）

动机：P41 后的逐目标归因显示残余未命中中有一部分位于低密度 M10 路由。先验证这条路由是否仍有必要，
避免把后续时间投入在一个可由 M26 统一替代的专家上。

验证：除去低密度二级 checkpoint 外，M26 epoch 003、P41（仅 `event_count > 30000`）、P6、P0/P0c 和
P18-global 均与正式协议完全一致。所有视频均直接使用 M26。

| 配置 | Score | Pd |
| --- | ---: | ---: |
| P41 正式基线（M10 + M26 路由） | **0.9638562171** | **0.9785804284** |
| M60 全部使用 M26 | 0.9605980921 | 0.9714405712 |

结论：**M10 epoch 002 是不可替代的低密度专家。** 取消路由会损失大量目标时间窗，不能以 M26 替代，
也不能从“统一模型”方向继续优化。原计划的 M61 低密度隔离续训虽然完成了路由代码和单测，但在训练前
被否决：它仍是同一 M10 路径的局部微调，无法满足当前需要结构性增益的门槛，故没有运行、没有产生
checkpoint。

相关日志：`log/analysis/m60_m26_allroutes_p41_full_verify.log`。

## M62：残余高速低分窗的冻结 H/4 局部对应诊断（2026-08-12，否决整条局部匹配路线）

动机：P41 后仍有 76 个原始分数不足的目标时间窗，其中 M26 路由的残余漏检具有较高相邻箱位移。若冻结
M26 的 H/4 特征在真实前一位置附近仍存在稳定局部对应，可考虑新的 cost-volume、可变形对齐或局部相关
记忆模块；若没有，则这些模块只会重复 M35/M40 的试错。

诊断：在完整 24 视频 P41 生产协议下，仅对 M26 路由的目标时间窗作离线报告。每个当前目标中心在前一
时间箱 H/4 特征图的 `9 x 9` 邻域（半径 4）计算余弦相似度，比较真实前一位置的排名、相对零位移的
优势和相对局部最优的差距。标签和 target id 只用于离线分组，推理规则不读取它们。

| H/4 对应统计 | 原始低分窗 | 已检出窗 |
| --- | ---: | ---: |
| 可在搜索窗内评估的数量 | 51 | 2397 |
| 输入位移中位数（px / 50） | 5.34 | 1.93 |
| 真实位置 top-1 | 13.7% | 52.6% |
| 真实位置 top-3 | 23.5% | 89.2% |
| 真实位置平均排名（81 候选，均匀期望 40） | 30.76 | 1.25 |
| 真实位置相对零位移相似度中位差 | -0.0518 | 0.0000 |

原始低分窗的真实对应只有 17.6% 优于零位移，且相对局部最佳的相似度中位差为 `-0.1236`。这说明对残余
高速目标，冻结 H/4 表征中的背景或错误位置通常比真实历史位置更相似；M39 中“背景 affinity 高于真目标”
和 M40 两个 seed 的冻结 H/4 训练无收益正好解释了这个现象。

结论：**不实现、不训练 cost-volume、局部相关、可变形对齐或另一条 H/4 运动记忆分支。** 这不是调小
学习率或更换 seed 能解决的问题，而是输入表征没有可利用的匹配信号。后续候选必须先通过同类可量化
诊断，避免把“与错误现象有关”误当成“模型可以从中学习”。

相关脚本与日志：`log/analysis/analyze_m62_raw_low_feature_matching.py`、
`log/analysis/m62_raw_low_feature_matching.log`。

## M63：原始多尺度运动持续性可分性诊断（2026-08-12，不采用）

动机：M62 排除了冻结特征的局部对应后，检查另一条机制不同的候选：直接从原始事件帧构造已有的
多尺度 motion-persistence 通道（每个时间邻居按半径 `1/2/4/8 px per bin` 做局部最大，再与中心箱
事件相交）。该信号不依赖 M26 特征匹配，理论上可能为高速低分目标提供输入级补充。

诊断：在完整 P41 生产协议下，对 M26 路由的目标中心和最终预测为正的背景事件，计算各尺度邻域支持
除以中心箱活动的比例。标签只用于离线分组，没有改变推理分数。用 rank AUC 比较原始低分真目标与最终
假阳性，方向只在诊断中报告，不据此选阈值。

| 半径（px / bin） | 低分真目标 n=57，中位数 | 正常真目标 n=2496，中位数 | 假阳性 n=1559，中位数 | 低分真目标 AUC | 方向性可分性 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 1.0000 | 1.0000 | 1.0000 | 0.3996 | 0.6004（低分更弱） |
| 2 | 1.0000 | 1.0000 | 1.0000 | 0.4601 | 0.5399 |
| 4 | 1.0000 | 1.0000 | 1.0000 | 0.4948 | 0.5052 |
| 8 | 1.0000 | 1.0000 | 1.0000 | 0.5112 | 0.5112（方向反转） |

该比例在绝大多数事件上饱和为 1；半径 1 的唯一非饱和信息还显示低分真目标比背景支持更弱，和想要
的“用运动持续性恢复低分目标”相反。它既不能提供稳定的无标签门控，也不支持在 M26 前端追加一个
多尺度运动适配器。

结论：**不实现、不训练 P25/P30 类原始运动持续性分支，也不扫描半径或阈值。** 至此，当前剩余错误
对应的三类低成本方向均有反证：扩张轨迹会引入假阳性（M28/M34）、冻结局部特征没有真实对应（M62）、
原始运动支持不能区分目标和背景（M63）。下一次若继续优化，必须引入与现有事件级概率校准不同的、可
通过独立视频或训练集留出验证的结构性信息；否则保持 P41 正式模型，避免继续消耗训练时间。

相关脚本与日志：`log/analysis/analyze_m63_raw_motion_persistence.py`、
`log/analysis/m63_raw_motion_persistence.log`。

## M64：历史 P53 25-bin 目标中心帧模型互补性复核（2026-08-12，不采用）

动机：M62/M63 已排除冻结局部对应和原始运动持续性。为避免继续只在 M26 同构分支上微调，回溯
`EV-UAV-main` 中结构明显不同的 P53：它以 `25` 时间单位、`9` 个上下文箱、`width=24` 的独立
时序帧网络预测事件分数，并使用目标中心热图和目标窗覆盖监督；当前主模型则是 `50-bin` 的双向
ConvGRU + attention + target-flow。旧分支曾在更早主模型上出现过极小的 P53 融合正增益，因此只做
一次严格、预先限定的互补验证，而不直接启动训练或权重扫描。

checkpoint：
`/mnt/d/AI/ESOD/EV-UAV-main/log/p53_25ms_center_ft50_seed37/runs/20260802-111726_seed37_pid5352/epoch_025_seed37.pt`。
现有 `load_temporal_frame_model()` 已在 CUDA 上严格加载其 metadata（`25-bin / context=9 /
width=24 / target_center=true`），没有 state_dict 兼容性问题。

验证协议：先用 **当前完整生产参数** 精确回归 M26 epoch 003 + M10 低密度路由 + P41 +
P6/P0/P0c/P18-global；然后只固定验证两组全局概率融合：`95% 当前生产分数 + 5% P53`、
`90% 当前生产分数 + 10% P53`。不按视频、事件数、标签或目标窗选择权重；P6 下必须和
`test2.py` 一致，在每视频后处理后先物化各自阈值的二值预测，再累计全局指标。

| 配置 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前生产基线（精确回归） | **0.9638562171** | **0.9785804284** | **0.9423022866** | **0.9763227701** | 4.6632902944e-06 |
| M26/P41 95% + P53 5% | 0.9627683172 | 0.9758504830 | 0.9412450194 | 0.9740634561 | 4.5092298781e-06 |
| M26/P41 90% + P53 10% | 0.9624211922 | 0.9752204956 | 0.9399140477 | 0.9714377522 | 4.3581321620e-06 |

P53 在基线最终漏检的 `102` 个目标时间窗中单独越阈值 `17` 个（M26 路由 9 个、M10 路由 8 个），
但不能转化为可用互补性：5% 融合仅恢复 1 个窗、同时损失 14 个已检出窗；10% 融合恢复 3 个、
损失 19 个。它主要压低背景误报（最终背景正事件分别减少 81、168），却同时压低真目标，导致
Pd 和总分显著下降。

结论：**永久停止 P53 及同类历史独立时序帧模型的 checkpoint 融合、权重/阈值扫描和续训。**
“少量残余窗可见”不足以说明融合可用；在当前高 Pd 条件下，候选必须首先证明它不会破坏大量已正确
目标。下一方向应检查当前 50-bin 表示中是否丢失了具有可分性的微时间信息，而不是再引入较弱模型
的全局概率平均。

相关脚本与最终日志：`log/analysis/analyze_m64_p53_complementarity.py`、
`log/analysis/m64_p53_complementarity_production.log`。

## M68：训练序列与完整流推理上下文失配诊断（2026-08-13，记录，不直接训练）

动机：M26 训练时只使用 `sequence_length=16` 个 50-unit 时间箱，但生产推理会对完整视频进行双向
ConvGRU 和记忆 attention。先量化长时上下文是否实质参与当前分数，避免把“更长训练序列”当作没有证据
的结构改动。

在固定 M10 低密度路由、M26 epoch 003、P41、P6、P0/P0c 和 P18-global 的完整 24 视频协议下，只将
M26 推理记忆截为不重叠的 16 或 32 bin 窗口：

| M26 推理上下文 | Score | Pd | 相对完整流 Score |
| --- | ---: | ---: | ---: |
| 完整视频双向流（生产） | **0.9638562171** | **0.9785804284** | -- |
| 16-bin 窗口 | 0.9611640581 | 0.9754304914 | -0.0026921590 |
| 32-bin 窗口 | 0.9621878265 | 0.9777404452 | -0.0016683906 |

结论：完整双向上下文确实重要，不能改回短窗推理；但该诊断只说明生产推理在利用长程记忆，**不构成把
训练窗口扩到 32 的充分证据**。后续任何长上下文训练必须保持 M26 概率校准，并以独立 seed 的完整验证为
准，不能以训练 loss 判断。相关脚本与日志：`log/analysis/analyze_m68_context_length_mismatch.py`、
`log/analysis/m68_context_length_mismatch.log`。

## M69：32-bin memory-only 微调（2026-08-13，否决）

基于 M68，只更新已存在的双向 ConvGRU、memory projection 和 attention，冻结 base、decoder、DACC、
flow 与所有后处理；从 M26 epoch 003 初始化，训练序列改为 32，两个独立 seed 均只训练 1 epoch。该设计
避免把长上下文尝试与新损失、新分支混在一起。

| 配置 | Score | 相对生产基线 |
| --- | ---: | ---: |
| M26 + P41 生产基线 | **0.9638562171** | -- |
| M69 seed 78 | 0.9638630076 | +0.0000067905 |
| M69 seed 79 | 0.9631441258 | -0.0007120913 |

seed 78 的变化远小于可接受的增益门槛，seed 79 则损失 `0.0016799664` Pd。两次结果方向不一致，说明
长序列的极小表面波动不具备稳定性，而且仍会扰动 P41/P0/P18 所依赖的概率分布。

结论：**停止 M69。** 不增加 epoch、不调学习率、不再换 seed，也不继续做 memory-only、32-bin/更长
训练序列的变体。相关文件：`log/analysis/run_m69_context32_memoryonly_e1_seed78.sh`、
`log/analysis/run_m69_context32_memoryonly_e1_seed79.sh` 及对应完整验证日志。

## M70：局部时间表面信息诊断（2026-08-13，否决）

动机：M65 的逐点微时间直方图未证明独立信息后，进一步检查同一 50-unit 时间箱内的空间-时间平面、
轨迹线性度和早晚质心位移，判断计数帧丢弃的事件先后顺序是否能区分残余低分目标与最终假阳性。

表面上 `time_plane_gradient` 的全局可分性为 `0.6930`，跨视频中位对齐 AUC 为 `0.6618`；但在严格匹配
11x11 局部事件计数后，所有时间顺序特征都退化到随机附近：gradient `0.5166`、plane R2 `0.5178`、
trace linearity `0.5010`。更关键的是 M26/P41 路由内 gradient 可分性只有 `0.6235`，未达到预设的
`>=0.65` 全局、`>=0.60` 计数条件化和 `>=0.62` 路由内的联合门槛。

结论：**不实现或训练 time-surface、timestamp image、voxel 时间顺序适配器，也不扫描微时间窗口或
时间切分。** 这些特征的表面相关性来自已有计数帧已经包含的局部活动量，不能作为新增泛化证据。相关文件：
`log/analysis/analyze_m70_local_time_surface.py`、`log/analysis/m70_local_time_surface.log`。

## M71：局部时间背景与空间紧致度可分性诊断（2026-08-13，M72 前置诊断）

动机：M65/M70 已否决逐事件微时间和同一时间箱内的时间表面特征，但二者均未显式检查“目标附近的
空间背景活动”是否是独立信息。对于当前生产协议最终漏检的低分目标，若其局部背景显著低于最终误报，
且该差异不能由中心局部事件数解释，则可以考虑一个只读取输入事件流的局部背景适配器；否则不能把表面
相关性当作新分支的依据。

诊断在完整 24 视频生产协议（M10 低密度路由、M26 epoch 003、P41、P6、P0/P0c、P18-global）下进行。
对每个事件，以固定 `11x11` 邻域和中心 `3x3` 核计算局部总计数、核心占比、占据率、峰值像素占比、
空间熵、均方根半径、邻域背景平均计数与峰值比。标签只用于离线分组；尤其对“残余低分真目标 vs 最终
假阳性”做了严格同局部计数匹配，并报告 M26/P41 路由内部结果。

| 特征 | 全局可分性（方向） | 跨视频中位 AUC | 同计数条件化 | M26/P41 路由内 |
| --- | ---: | ---: | ---: | ---: |
| `context_mean_count` | **0.7816（残余目标更低）** | **0.9310** | **0.7511** | **0.7369** |
| `core_fraction` | 0.7266（残余目标更高） | 0.7912 | 0.5453 | 0.7007 |
| `local_event_count` | 0.7152（残余目标更低） | 0.7250 | 0.5000 | 0.6512 |
| `occupied_fraction` | 0.7047（残余目标更低） | 0.7208 | 0.5167 | 0.6308 |

`context_mean_count` 是唯一同时通过全局、跨视频、严格计数条件化和 M26/P41 路由内门槛的描述符；它说明
残余低分真目标更常出现在相对安静的邻域中。可是，把已检出目标与最终假阳性做同计数条件化后，该特征
的可分性只有 `0.5025`。因此它没有证据支持全局抬高目标分数，也没有证据支持一个无标签阈值后处理，
只能作为“冻结主模型、零初始化、仅检验局部背景是否能修复残余目标”的保守适配器输入。

结论：允许一次严格受限的零初始化适配器探针（M72），但**不**据此扫描阈值、后处理规则或修改生产路径。
若该适配器不能在不改变 Pd/Fa 的前提下提高 IoU，则这条局部背景路线整体否决。

相关脚本与日志：`log/analysis/analyze_m71_local_spatial_context.py`、
`log/analysis/m71_local_spatial_context.log`。

## M72：零初始化局部时间背景适配器（2026-08-13，不采用）

动机：M71 的 `context_mean_count` 对残余低分目标具有跨视频、同局部计数条件化后的稳定可分性。M72
不改变 M26 的双向记忆、attention、target-flow、DACC 或后处理，而是在输入时序帧旁增加一个由相邻
时间箱 `11x11` 局部背景统计构成的通道，并以零初始化残差适配器读取该通道。这样可以测试局部背景信号
能否修复低分目标，同时避免重新扰动当前已校准的主干。

实现与安全验证：

1. `dataset/temporal_frame.py` 构造邻近时间箱的局部背景通道，`dataset/temporal_memory.py` 在训练期
   随序列传递该通道；完整流推理路径也由 `utils/temporal_memory_inference.py` 根据 checkpoint metadata
   自动恢复。
2. `TemporalFrameNet` 提供共享 `encode_features()`，新增零初始化的局部背景 adapter；
   `TemporalMemoryNet` 支持 11 通道输入。
3. `train_temporal_memory.py` 新增只训练该 adapter 的模式。单元测试
   `tests/test_m72_local_temporal_context.py` 覆盖零扰动、adapter 梯度以及 optimizer 参数范围。
4. 从 M26 epoch 003 制作零挂接 checkpoint 后，完整 24 视频端到端验证逐项严格复现生产基线：
   `Score=0.9638562171`。因此训练后的变化可以归因于新增 adapter，而不是 checkpoint 加载或推理路径漂移。

正式探针从 M26 epoch 003 初始化，只训练 adapter 一轮：54 个高密度训练视频、每视频 6 个确定性视图，
共 324 个序列；训练约 3 分 44 秒。完整验证固定 M10/M26 路由、P41、P6、P0/P0c 与 P18-global：

| 配置 | IoU | Acc | Pd | Fa | Score |
| --- | ---: | ---: | ---: | ---: | ---: |
| P41 正式基线 | **0.9423022866** | **0.9763227701** | **0.9785804284** | **4.6632902944e-06** | **0.9638562171** |
| M72 adapter e1 | 0.9422581196 | 0.9762769938 | **0.9785804284** | **4.6632902944e-06** | 0.9638428060 |

M72 比基线低 `0.0000134111`，Pd/Fa 完全不变，IoU 与 Acc 同步轻微下降。该结果与 M36/M40/M48 的共同
形态一致：局部信号虽然能描述“残余低分目标 vs 最终误报”，却没有为“已检出目标 vs 最终误报”提供额外
可分性，训练后只能轻微扰动现有概率排序，无法改变最终检测决策。

结论：**停止 M72，不延长训练、不换 seed、不调学习率，也不扫描局部背景阈值或 adapter 宽度。** 至此，
微时间（M65/M70）、局部运动（M62/M63）和局部背景统计（M71/M72）三类输入级补充均已通过诊断后仍未
转化为可靠增益。下一轮研究必须转向与“单事件置信度微调”不同的误差来源，例如监督目标与官方评分之间
的结构性失配或跨视频域的可观测分布；不能继续堆叠局部特征残差。

相关日志：`log/analysis/m72_zero_attached_full_verify.log`、
`log/analysis/m72_local_temporal_context_e1_seed80_train.log`、
`log/analysis/m72_local_temporal_context_e1_seed80_full_verify.log`。

## M73：历史 width=24 时序记忆模型互补性诊断（2026-08-13，不采用）

动机：当前 M26 是 `width=16 + attention + target-flow`，而早期 `EV-UAV-main` 留有三种
`width=24 + target-centre + centre-memory` 的独立 checkpoint。即使旧模型的绝对分数较低，也可能对
当前 P41 最终漏检窗存在结构互补；因此不直接训练或扫描融合权重，而是先缓存当前完整生产分数，逐事件
对齐后检查“能恢复多少正式漏检”与“会丢失多少现有命中”。

所有诊断固定 M10 低密度路由、M26+P41 高密度路由、P6、P0/P0c、P18-global，不按视频名选择模型。
预先定义的准入条件为：历史模型原始分数至少覆盖 `12` 个当前漏检窗、跨至少 `4` 个视频，且经过同一
后处理后的恢复数必须大于损失数；不满足则禁止进行任何概率融合或继续训练。

| 历史 checkpoint | 原始覆盖的当前漏检窗 | 最终恢复 / 丢失目标窗 | 覆盖视频数 | 结论 |
| --- | ---: | ---: | ---: | --- |
| P42 center-memory e20 | 18 | 6 / 164 | 5 | 否决 |
| P47 dense hard-negative e50 | 24 | 13 / 131 | 7 | 否决 |
| P49 metric-aligned e05 | 19 | 5 / 169 | 4 | 否决 |

P47 是三者中原始覆盖最多的模型，但仍需以约十倍以上的已命中目标为代价换取少量恢复，说明其概率校准
与当前 M26/P41 不兼容。结论：**永久停止这些历史 width=24 checkpoint 的融合、阈值扫描与续训。**
相关文件：`log/analysis/m73_historical_width24/`。

## M74：P0 组件级质量校准交叉验证（2026-08-13，不采用）

动机：M66 显示 P0/P0c 保留组件的空间半径、累计位移、持续时间和分数统计可在验证标签上区分含目标
组件与纯背景组件。该方向不同于 M36 的事件级置信残差：它只在 P0 已保留的组件上做二值保留决策，理论上
可降低官方 Fa 组件数而不触碰原始网络。

严格协议：先对 `99` 个训练视频运行冻结 M10/M26+P41 前向并缓存原始分数；在训练视频层面做 `5` 折
OOF（整视频留出）L2 logistic 组件分类，特征仅包括 P0/P0c 后可观测的组件事件数、时长、cell 数、
分数统计、空间范围、逐箱运动和 P0c 标记。验证标签不参与拟合。初始门槛预先固定为
`p(target component) >= 0.05`，只有 OOF 同时保持 Pd 且降低 Fa，才允许一次冻结模型的验证集回放。

OOF 的最保守门槛 `0.05` 不删除任何训练目标组件，Fa 从 `1.1408e-05` 降至 `1.1279e-05`，但只移除
`7` 个组件。冻结训练集模型用于验证集时没有任何组件低于该门槛，所有指标与生产基线严格相同：
`Score=0.9638562171`、`Pd=0.9785804284`。更高的 OOF 门槛立即删除真目标：`0.10` 已删除 `3` 个，
`0.20` 删除 `8` 个，`0.50` 删除 `42` 个，并伴随 Pd 单调下降。因此不存在既有效又不牺牲召回的门槛。

结论：**不把组件分类器接入生产，不用验证集调门槛，不实现组件质量后处理。** 这再次说明 M66 的组件
可分性主要来自 P0 已使用的规模/持续性信号，无法形成稳定的额外泛化规则。相关文件：
`log/analysis/cache_m74_production_train_p41_scores.py`、
`log/analysis/analyze_m74_component_quality.py`、
`log/analysis/m74_component_quality/`。

## M75：残余轨迹候选图路线停止（2026-08-13，不采用）

M75-A/B/C 的目标是在不改动 M10/M26/P41/P6/P0/P18 已有正预测的前提下，只对最终负事件提出极少量单调 OR 的候选 seed。该路线可以恢复一些遗漏窗口，但无法在严格视频级 OOF 中控制新增的背景连通域：完整可观测特征版本的 pooled OOF 虽恢复 `103` 个窗口，Score 仍下降 `0.0016313`；加入图连通和 Fa 预算后，所有 fold 都退回不接受任何 seed，净增为零。

后续 M75-C 图网络单个 epoch 的估计耗时约两小时，完整五折两 seed 预计约十四小时；在 M75-B 已无正向 OOF 证据的情况下，不具备继续训练价值。停止 M75 及其候选图、轨迹恢复、M75-T 无标签自适应的所有后续变体，不能以降低候选阈值、增加图半径、增加训练 epoch 或更换 seed 绕过该结论。

相关报告：`log/analysis/m75b_complete_oof_report.json`、`log/analysis/m75b_complete_oof_v2_report.json`。

## M76：外部 M20 高密度固定融合（2026-08-13，不采用）

队友 EVC 仓库的固定 M10/M20 方案完整复现为 `Score=0.9628776542`，基础分数低于当前 M26。仅在高密度路由使用固定全局概率融合 `M26 75% + M20 25%`，并保持 M10、P41、P6、P0/P0c、P18-global 不变，得到 `Score=0.9637654404`，低于正式基线 `0.9638562171`。

结论：外部 M20 对当前 M26 没有可用互补性；停止历史/外部模型固定概率融合。相关日志：`log/analysis/m76_external_m20_global_blend_w075_full_verify.log`。

## M77：训练分数分布驱动的无标签阈值校准（2026-08-13，不采用）

比赛最终测试视频会公开但标签不公开，因此尝试一个严格不读视频名和标签的策略：测试时只观察 event count、score p50/p90/p99、越过当前路由阈值的比例和 score excess；训练标签仅用于训练视频上拟合固定的阈值选择规则。候选阈值预先固定为 `0.700/0.710/0.718/0.7226/0.730/0.740/0.750`，P0/P0c/P18 与生产协议一致。训练和验证均使用冻结生产分数缓存，阈值对应的官方充分统计一次性缓存，避免重复推理。

训练域自身会偏向 `0.750`：`Score 0.9256643 -> 0.9290504`；但这是源域校准差异，不能迁移。将只由训练视频拟合的规则应用到公开验证视频时，所有 descriptor 分箱策略均退化到高阈值，结果为 `Score 0.9638562 -> 0.9635656`，下降 `0.0002906`，Pd 从 `0.9785804` 降为 `0.9781604`。

结论：停止固定阈值网格、event-count 分箱、分数分位数分箱、正事件比例和 excess 分箱等无标签阈值自适应；不能根据最终测试视频的分数分布重新选阈值。脚本与缓存：`analyze_m77_unlabeled_calibration.py`、`log/analysis/m77_unlabeled_calibration/`。

## M78：相似训练视频 KNN 无标签阈值迁移（2026-08-13，不采用）

M77 失败后进一步检验“测试视频公布”能否放宽为跨视频相似性迁移，而非全局训练阈值。每个视频仅使用可见的 event count、活跃 50-bin 比例、每 bin 事件数分位数、空间占用率、score p50/p90/p99、正分数比例和 excess 构成描述符；只在同一 M10/M26 路由中使用 7 个最近训练视频决定固定候选阈值。描述符标准化、OOD 回退半径、五折划分都只由训练视频的可见统计产生，外层视频标签在作出决定前不参与。

训练集五折 OOF 表面上稳定为正：`Score 0.9256643 -> 0.9289372`，五折 delta 均为正；但这主要复现了训练域普遍偏好 `0.750` 的现象。用全部训练视频作为冻结参考应用到公开验证视频后，得到 `Score 0.9638562 -> 0.9635519`，下降 `0.0003043`，Pd 从 `0.9785804` 降为 `0.9779504`。所有 24 个验证视频均在 OOD 半径内，故失败不是保守回退造成，而是相似度不足以迁移标签侧最优阈值。

结论：永久停止 KNN、聚类、特征距离或更丰富无标签视频描述符驱动的阈值选择。M77/M78 共同说明，当前训练/验证域的概率校准差异不能由无标签统计可靠修复；后续若利用公开测试视频，必须改变模型表示或学习过程，而不是继续后处理选阈值。脚本：`analyze_m78_knn_unlabeled_threshold.py`。

## M79：P41 双视图一致性驱动的无标签逐视频权重选择（2026-08-13，不采用）

动机：P41 是目前唯一稳定的无训练增益，但其高密度 M26 路由固定采用原时间栅格与 `+25` 半时间箱
栅格的 `0.75 / 0.25` 概率平均。仅检验一个严格受限的假设：能否只用两个冻结视图的可观测分歧和分数
分布，自动在此前已经评估过的原流权重 `0.50 / 0.75 / 0.90` 中逐视频选择，不读视频名、标签、target id
或额外测试视图。

协议：高密度 54 个训练视频固定索引 5 折 video-level OOF。描述符只含 `log1p(event_count)`、阈值附近与
全局双视图绝对差、差值方向、越阈比例、分数 p90。每个 outer fold 只用其余训练视频拟合 L2 ridge 来预测
`0.50`、`0.90` 相对默认 `0.75` 的单视频收益；预测收益必须至少 `1e-4` 才允许替换默认权重。预先固定
准入条件：pooled OOF Score `>= +0.00010`、至少 4/5 折不降、**Pd 不下降**。不满足则禁止运行验证集回放
或修改描述符/门槛/候选权重。

| 项目 | 数值 |
| --- | ---: |
| 训练 OOF 默认 P41 Score | 0.9104271729 |
| 训练 OOF 策略 Score | 0.9112735139 |
| Score 变化 | +0.0008463410 |
| Pd 变化 | **-0.00019736** |
| 非降 Score folds | 5 / 5 |

五折 Score 变化为 `+0.0001592 / +0.0003439 / +0.0001669 / +0.0043935 / +0.0012982`，但前三折 Pd 分别下降
`0.0004548 / 0.0019670 / 0.0010417`。策略只是从训练域中更常选择 `0.50` 获利，同时不稳定地选择 `0.90`，
不能承受未知测试集的召回风险。

结论：停止 P41 逐视频权重选择、双视图分歧阈值、熵/分位数调权及其 KNN/聚类变体。因预先定义的 Pd
条件失败，**没有运行验证集策略回放**。M77/M78/M79 一致表明公开测试视频的无标签输出统计不能可靠决定
当前已校准生产模型的后处理或融合决策。相关文件：`log/analysis/cache_m79_val_p41_views.py`、
`log/analysis/analyze_m79_phase_consistency.py`、`log/analysis/m79_val_p41_views.pt`。

## M80/M81：P41 双视图分歧诊断与组件门控（2026-08-13，不采用）

M80 检查 M26 原始时间栅格与 P41 `+25` 相位栅格的逐事件绝对分歧，是否能在当前 P41/P0/P18 已保留
事件中区分真目标和背景误报。为避免分歧只是重复已有置信度，将事件按生产分数带分别计算 AUC，再按
目标/背景 pair 数加权。结果为训练集 pooled AUC `0.8679`、严格分数条件化 AUC `0.6351`；验证集对应
`0.8884 / 0.6375`。但 `0.718--0.95` 的各分数带方向不稳定，强分离主要来自 `>=0.95` 的高置信区域，
不能把它当作低分误报删除依据。

M81 只做一次冻结组件门控复核：严格复用 M74 的 P0/P0c/P18 回放和 `p(target component) >= 0.05`
门槛，仅追加组件内分歧的均值、p90 和最大值。它使用冻结的 M75 五折视频划分
`m75_video_folds.json`（sha256 `f3700dc4178974b219d53ac1e61761a19edc39475f25bd0ad8db44eb867bb6b6`），
不从验证集拟合规则。

| 回放 | Score | Pd | Fa | 删除组件 |
| --- | ---: | ---: | ---: | ---: |
| 训练 OOF 基线 | 0.9256642714 | 基线 | 1.1407994083e-05 | 0 |
| 训练 OOF M81 | 0.9258192091 | 不变 | 1.1372128910e-05 | 4 个背景、0 个目标 |
| 验证集 M81 | 0.9638562171 | 0.9785804284 | 4.6632902944e-06 | 0 |

结论：**不接入生产、不扫描 `0.05` 门槛、不继续做 P41 分歧组件后处理。** 该特征只能在训练集极少数
背景组件上触发，在独立验证视频上没有可用动作。相关脚本：
`log/analysis/analyze_m80_phase_disagreement.py`、`log/analysis/analyze_m81_phase_component_gate.py`。

## M82：已有 DACC 的受限无标签测试时适配（2026-08-13，不采用）

考虑到评测阶段公开视频但标签不公开，M82 唯一尝试的是**模型适配而非阈值选择**。M26 checkpoint 的
`base.density_calibrator` 结构为一个密度 encoder 和 `1 -> 1 -> 16` 通道 gate；读取 checkpoint 确认两个
gate 线性层都严格为零，因此原模型等价于固定 `sigmoid(0)=0.5` 通道缩放。M82 冻结 M26 主干、双向
ConvGRU、attention、target-flow、P41、P6、P0/P0c、P18，只临时优化既有最后一层的 `16` 个 gate 参数。

每个高密度视频只用原流与 `+25` 相位流：目标为减少两视图分歧，同时对两者的发布分数施加固定锚定，
加入 L2 正则；固定为 `8` 步、lr `0.1`、anchor `25`、L2 `0.01`，且要求分歧至少下降 `1%`、平均/最大
分数改变量分别不超过 `0.003/0.03` 才接受。标签不进入适配；仅在最后用缓存以与 `test2.py` 一致的
P6/P0/P18 与 Challenge 2 evaluator 回放。

14 个 M26/P41 验证视频全部被拒绝：每个视频的 gate 参数虽可更新到 L2 `0.023--0.035`，但 DACC
`density_encoder` 经 ReLU 后的全局密度标量为零，故 gate 对全部事件的实际输入始终为零；原/相位分歧和
分数均不变（最大数值改变量至多 `1e-5`）。缓存回放严格复现并保持：

```text
IoU=0.9423022866  Acc=0.9763227701  Pd=0.9785804284
Fa=4.6632902944e-06  Score=0.9638562171
```

结论：**永久停止以发布 M26 的 DACC 作为测试时适配入口**；增加适配步数、学习率或无标签损失都不能
绕过“密度标量恒为零”的结构性退化。该结果不修改正式 checkpoint、`test2.py`、提交代码或 Git。相关脚本：
`log/analysis/inspect_m26_calibrator.py`、`log/analysis/analyze_m82_dacc_test_time_adaptation.py`。

## M83：原始局部事件体候选判别器单折筛查（2026-08-13，不采用）

动机：M75-B 已否决以 M26/P41 分数、局部计数、几何和轨迹特征恢复生产掩码之外的候选，但这不必然否定一个
直接观察原始 `x/y/t/polarity` 局部时空体的独立候选判别器。M83 不改 M10/M26/P41、阈值或后处理，也不重启
M75 的候选策略；只用冻结 M75-A 的候选宇宙、P0/P0c/P18 production mask、图连通每箱最多一个 seed 约束、
五折视频划分和虚警连通域计数。CNN 输入是候选周围的 `2 polarity x 9` 个 `25-unit` 时间片 x `11x11` 空间
patch，训练/推理均不包含分数、标签、target id、视频名或视频索引。

为避免重新做 5 折长训练，先固定 `outer fold=0`、`calibration fold=1`、其余三折训练。训练样本为 `2901`
个正候选与固定 `14505` 个负候选；小型 `Conv3d(2-12-20-24)` 固定 seed 83、6 epoch。校准折只能在固定
`0.05..0.95` 阈值中选择同时满足 `Score delta >= +0.00020`、至少恢复 3 个窗、每恢复窗新增 false component
不超过 4 的路由阈值；外折不参与选择。进入完整五折的预先固定条件还要求至少一条路由比 M75-B observable
logistic AUC 高 `0.03`、外折 Score 至少 `+0.00050`、恢复至少 5 个窗。

单折实际运行约 251 秒，patch 构建和 CNN 训练均正常，但独立外折候选 AUC 明显低于已否决的 M75-B 特征：

| 路由 | M83 原始事件体 CNN AUC | M75-B observable logistic AUC | 差值 |
| --- | ---: | ---: | ---: |
| M10 | 0.88172 | 0.93395 | -0.05222 |
| M26 | 0.82289 | 0.83765 | -0.01476 |

两个路由在 calibration fold 均没有满足风险预算的阈值，外折严格回退生产 mask，`Score delta=0`、恢复 `0` 窗。
这不是“阈值不够低”问题：更宽松的 M75-B 已在完整 OOF 中以 `103` 个恢复窗换来 `-0.0016313` Score，M83
又证明原始局部事件体不能提供比这些可观察特征更强的候选排序。

结论：**永久停止 M75 候选恢复的局部 CNN/3D patch/patch transformer/更多 epoch、负样本比例、patch 尺寸和
阈值变体。** 不跑五折，不接入生产，不修改 README、checkpoint、提交或 Git。相关文件：
`log/analysis/m83_local_volume_candidate_screen.py`、
`log/analysis/m83_local_volume_candidate_screen_report.json`、
`log/analysis/m83_local_volume_candidate_screen.log`。

## M87：训练期受控目标轨迹速度增广（2026-08-15，不采用）

动机：M26/P41 后剩余漏检集中在高速、小目标，但 M31 只改变快速时间箱的抽样频率，破坏了原有概率
校准。M87 改为对训练集已有的一个真实目标轨迹施加固定残余速度：保留原始事件的极性、时间、形状和
标签，只在序列及其上下文范围内沿该目标观测方向平移坐标；背景事件不变，越过传感器边界的候选严格
回退原样。开关默认关闭，推理和测试集不读取标签。

训练前数据审计：训练集有 `19,467` 个相邻目标轨迹对，其中 `3,255` 对速度至少 `4 px/50`、`291` 对
至少 `8 px/50`；验证集对应为 `4,578/502/48`。数据集级检查在 408 个训练序列中成功产生 194 个
可行增广，0 个坐标越界或非有限值，关闭开关时样本逐位等价。实现文件为
`dataset/trajectory_augmentation.py`，并接入 `dataset/temporal_memory.py`；配置默认关闭。

固定冒烟配置：从正式 `M26 epoch 003` 初始化，保持原始 BCE、target-flow、attention、dense 8x 和全部
生产后处理不变；只在 `event_count > 30000` 的额外视图以 `0.50` 概率增广，残余速度 `4 px/50`，至少
连续 3 个目标时间箱，seed `87`，训练 1 epoch。训练完成 408 序列，实际增广 91 个视图，target-flow
有效对 `8,946`，无 NaN/Inf；训练约 8 分 44 秒。checkpoint：
`log/m87_traj_aug_m26e3_e1_seed87/runs/20260815-013127_seed87_pid451/epoch_001_seed87.pt`。

完整 `test2.py` 回放固定正式 M10/M26 路由、P41 `0.75/0.25`、P6 `0.718/0.7226`、P0/P0c/P18-global：

| 配置 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: |
| 正式基线 | **0.9638562171** | **0.9785804284** | **0.9423022866** | **0.9763227701** | **4.6632902944e-06** |
| M87 e1 seed87 | 0.9631172103 | 0.9766904662 | 0.9421308637 | 0.9759869576 | 4.6336632912e-06 |

`Score` 下降 `0.0007390068`，`Pd` 下降 `0.0018899622`；Fa 的微小下降不足以补偿真实目标损失。
按预注册门槛停止 M87，不增加 epoch、不换 seed、不扫描残余速度/概率，也不接入正式 checkpoint、README
或 Git。当前研究代码仍保留，但默认关闭，仅供追溯。

补充一次与训练机制一致的密集路由回放：M87 只在训练时对 `event_count > 200000` 的额外视图施加增广，
因此固定让 `>200000` 的推理视频完全使用 M87，其余视频严格使用正式 M26/M10；不做按视频选择或权重扫描。

| 配置 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: |
| 正式基线 | **0.9638562171** | **0.9785804284** | **0.9423022866** | **0.9763227701** | **4.6632902944e-06** |
| M87 仅 `>200000` 密集路由 | 0.9638457359 | 0.9785804284 | 0.9422261119 | 0.9762006402 | 4.6573648938e-06 |

结果低 `0.0000104812`，没有达到任何有效增益门槛；即使只影响两个高密度验证视频也不能证明普适价值。
至此停止 M87 的全路由、密集路由和任何融合变体。

## M88：全画幅连续 SparseConv3D 外层筛查（2026-08-15，待训练）

M87 的失败再次说明，在已很高的 `Pd` 上对 M26 做训练期微扰通常只会损失真实目标，不能再继续叠加小损失、
抽样或后处理变体。M88 因此不读取 M26、候选分数、视频名或外部 checkpoint，而是对完整传感器的原始
`x/y/t/polarity` 以 `10-unit` 体素构造连续 `400-unit` 片段，用独立的全画幅 SparseConv3D U-Net 输出每个
事件的 logit。它的目标不是直接替换正式链路，而是先证明原始时空表征在未参与训练的视频上有独立判别力。

工程实现：`dataset/fullframe_sparse3d.py`、`model/fullframe_sparse3d_net.py`、`train_fullframe_sparse3d.py`；
CUDA smoke 已完成（`34,906` voxels，`222,449` 参数，峰值 `142 MiB`）。本次补全了
`log/analysis/eval_m88_sparse3d_outer.py`：主前向只读取坐标、时间和极性；每个事件由无重叠 400-unit clip
恰好覆盖一次；主前向完成后才读取 outer 标签计算 ROC-AUC/PR-AUC；checkpoint 的训练视频须和固定 M75
外折完全一致；每个视频还会将标签翻转并断言体素输入和 logits 逐位不变。脚本已通过 `py_compile` 与 EV39
环境导入检查，尚未训练或得到任何精度结果。

固定第一步是 outer fold 0、3 epoch、79 个训练视频，预计超过 20 分钟，必须由用户运行文档中的训练命令。
只有 fold 0 与 fold 2 均达到 `ROC-AUC >= 0.90`、`PR-AUC >= 0.60`、至少 70% 视频 `AUC >= 0.80` 且显存
不超过 `3.2 GiB`，才允许讨论五折 OOF 或与 M26 的全局校准融合。任一门槛失败立即停止 M88，不延长 epoch、
不换 seed、不改模型宽度或体素尺寸，也不在公开 val 上扫阈值补救。

### M88 实际结果与否决结论（2026-08-15）

固定 `seed=88`、3 epoch、每视频 4 views 完成五折整视频 OOF。单折训练约 75--85 秒，推理约 12 秒；99 个
视频、`9,324,544` 个事件均有且仅有一个 OOF logit，所有标签翻转不变性、checkpoint 训练视频隔离和显存检查通过。

| outer fold | ROC-AUC | PR-AUC | 视频 AUC>=0.80 比例 |
| ---: | ---: | ---: | ---: |
| 0 | 0.959543 | 0.653823 | 1.00 |
| 1 | 0.942872 | 0.748493 | 0.90 |
| 2 | 0.971701 | 0.827755 | 0.95 |
| 3 | 0.947705 | 0.750370 | 0.95 |
| 4 | 0.952137 | 0.754987 | 1.00 |

判别门槛全部通过，但高 AUC 没有转化为 Challenge 分数。完整 OOF 的固定评分为：

| 路线 | Score | Pd | Fa |
| --- | ---: | ---: | ---: |
| M88 概率阈值 0.50，无后处理 | 0.4883043172 | 0.9353167555 | 8.1533731353e-04 |
| M88 阈值 0.50 + 正式 P0/P0c | 0.5243443728 | 0.8848924413 | 2.3108934895e-04 |
| M88 高置信阈值 0.95，无后处理 | 0.7137328351 | 0.6350404579 | 6.0375431462e-06 |

最后检查其作为 M10/M26 残差候选的可能性：在正式阈值以下取 `M88>=0.95`，共得到 `49,696` 个候选，只有
`470` 个正事件，精度 `0.9458%`，仅覆盖主模型漏标正事件的 `7.67%`；高密度视频精度更低至 `0.1043%`、
覆盖率 `1.20%`。继续提高阈值会进一步牺牲召回，降低阈值只会增加已经过高的连通虚警。

结论：**M88 正式否决。** 不替换 M10/M26、不做概率融合、不做残差恢复、不接入 P0/P18；停止 threshold、
epoch、width、voxel 尺度、seed 和更多后处理扫描。当前正式最高分仍为 `0.9638562171`。

### M88-B：M88 固定否定证据门（2026-08-15，不采用）

尽管 M88 不能独立检出目标，仍检查了一个比概率融合更保守的派生假设：对于它 OOF 判别相对可靠的低/中
密度视频（`event_count <= 200000`），仅当 M88 概率小于 BCE 的语义边界 `0.50` 时，将冻结
M10/M26+P41 的对应事件分数置零；高密度 M26/P41、P6、P0/P0c 与 P18-global 保持原样。该规则不使用
视频名、target id、验证标签或可调阈值。所有 99 个训练视频均使用“该视频不参与 M88 训练”的五折 OOF
logit，并按完整 Challenge 指标和同一五折重新计分。

| 配置 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: |
| 冻结训练生产基线 | **0.9256642714** | **0.9635879218** | **0.8770307899** | **0.9716695547** | 1.1407994083e-05 |
| M88 `p<0.50` 低/中密度否定门 | 0.9054714378 | 0.9220446023 | 0.8586615920 | 0.9449405670 | **1.0377946328e-05** |

门虽然使 Fa 下降，却剔除了 `8,477` 个真实正事件，Pd 下降 `0.04154`、Score 下降 `0.02019`；五个外折均为
负增益（`-0.02581/-0.02154/-0.01269/-0.03080/-0.00821`）。它没有通过预先固定的“Score 增益
`>=0.00020`、Pd 不降、Fa 降、至少四折正增益”任一核心门槛。

结论：**停止 M88 的否定门、事件筛除和任何基于其 logit 的后处理。** M88 的排序相关性不足以承受高 Pd
区域的漏检成本，不能再以不同概率阈值、密度边界或组件规则绕过这次失败。脚本与结果：
`log/analysis/analyze_m88_oof_negative_gate.py`、`log/analysis/m88_oof_negative_gate_report.json`。

## M89：原始多速度运动补偿累计诊断（2026-08-15，不采用）

M62/M63 已否决冻结 H/4 特征对应和零位移局部持续性，但这两者都没有把高速目标在相邻时间箱内沿候选
速度直接对齐。M89 因此仅做无训练筛查：在原始完整分辨率事件计数上，对中心时间箱前后各两个 50-unit
时间箱，沿 `[-18,18] px/50`、步长 `2` 的二维速度网格累计 `3x3` 局部事件量；以“最大运动对齐累计
减去零位移累计”的 `log1p` 增益作为唯一特征。速度范围由此前残余漏检的约 `p99=18.9 px/50` 预先固定，
不从验证标签选择。

诊断只读取冻结训练集 M10/M26+P41 分数与正式 P0/P0c/P18-global，标签仅用于比较 `524` 个最终未检出的
raw-low target window 与 `17,921` 个最终背景阳性 3x3 时空单元；不训练分类器、不读验证数据、不改变模型或
后处理。准入要求为：全局 AUC `>=0.66`、跨视频中位 AUC `>=0.60`、方向一致性 `>=70%`、按零位移支持量
条件化 AUC `>=0.60`、M26/P41 路由 AUC `>=0.62`。

| 指标 | 全部 | M10 | M26/P41 |
| --- | ---: | ---: | ---: |
| 全局 AUC（残余 target 增益更高） | 0.5064 | 0.6644 | 0.5163 |
| 跨视频中位 AUC | 0.6233 | 0.6642 | 0.5829 |
| 零位移支持量条件化 AUC | 0.2800 | 0.3599 | 0.2807 |

虽然残余目标的候选最佳速度中位数为 `11.66 px/50`（M26/P41 为 `14.07`），符合高速误差画像，但背景单元在
更丰富事件量下更容易被任意速度路径累加；条件化后所有区间的 AUC 都明显小于 `0.5`。该统计因此没有新增
可迁移的运动证据，反而会放大背景相干噪声。

结论：**不实现 raw motion-compensation frame、Hough velocity bank、motion-aligned attention 或基于其的
训练/后处理。** 不扩大速度范围、时间上下文、局部半径或速度步长。脚本与结果：
`log/analysis/analyze_m89_raw_motion_compensation.py`、`log/analysis/m89_raw_motion_compensation_report.json`。

## M90：公开视频目标域的冻结特征偏移审计（2026-08-15，不训练）

考虑评测阶段会公布测试事件流但不公开标签，曾考虑用训练集标签监督、公开测试事件流无标签的域对抗训练。
在启动任何 DANN/域对齐训练前，先用冻结 M26 checkpoint 对高密度（`event_count > 30000`）训练和验证视频
各均匀抽取 8 个时间帧，提取 H/4 与 bottleneck 的空间均值/标准差特征。域分类器只在整视频交替留出划分上
训练和评估；不使用 segmentation label、target id、视频名或预测分数。另将每个特征对当前时间箱
`log1p(event_count)` 的线性影响去掉，避免把已知密度差异当成可迁移的域偏移。

| 域探针 | 留出划分 0 | 留出划分 1 | 最小 AUC |
| --- | ---: | ---: | ---: |
| 原始冻结特征 | 0.5823 | 0.5203 | 0.5203 |
| 去除中心事件量后的特征 | 0.5681 | 0.5122 | 0.5122 |

两种 probe 都接近随机，未观察到可被域对齐消除的稳定非密度偏移。该结果不是“域对齐训练失败”，而是
训练前提不成立；继续增加 domain head、对抗权重、epoch 或改用验证标签都会变成无证据的实验。

结论：**停止 M90 域对抗/跨域无标签训练路线。** 若后续正式测试事件分布确实出现明显新域，应重新做同样的
冻结特征审计，不得直接复用当前训练方案。脚本与结果：
`log/analysis/analyze_m90_transductive_domain_shift.py`、`log/analysis/m90_transductive_domain_shift_report.json`。

## M92：共享全局背景位移下的相对运动诊断（2026-08-15，不采用）

M89 否决的是“每个局部候选各自从速度网格选最大累计”：背景事件也能各自选择最优速度，因而其累计增益
比漏检目标更高。M92 因此先以原始完整分辨率事件计数构成相邻 50-unit 时间箱的整幅图，在固定
`+/-12 px/50` 搜索范围内用 `2x2` 池化互相关估计**唯一共享**的背景位移；再在候选点周围计算相对于该
背景位移的 `+/-18 px/50` 残差速度累计增益。它与 M89 的关键区别是背景位移对整段视频所有位置相同，
局部候选不能自己选择绝对速度。推理时只依赖测试可见事件流，不使用视频名、target id、模型特征、验证
数据或标签。

本轮仍只是准入诊断：读取冻结的 99 视频 `train_p41_scores.pt`，标签仅用于在所有原始事件特征计算后比较
`524` 个最终未检出的 raw-low target window 与 `17,921` 个最终背景阳性 3x3 时空单元；不训练、不改模型、
不扫描验证参数。由于瓶颈在 M26/P41 路由，预先要求其同时满足：全局 AUC `>=0.62`、逐视频中位 AUC
`>=0.60`、方向一致性 `>=70%`、控制全局对齐支持量后的 AUC `>=0.60`，且 raw-low 样本不少于 `200`。

| 指标 | 全部 | M10 | M26/P41 |
| --- | ---: | ---: | ---: |
| 相对运动增益全局 AUC（target 更高） | 0.4643 | 0.4658 | **0.4912** |
| 跨视频中位 AUC | 0.4871 | 0.4341 | **0.5285** |
| 跨视频方向一致性 | 48.00% | 41.67% | **53.85%** |
| 控制背景对齐支持量后的 AUC | 0.2629 | 0.2689 | **0.2715** |

虽然原始全局估计有非零位移（相邻时间箱平移模长 p50/p90 为 `2/6 px`），其共享背景补偿并没有使漏检目标
相对假阳性更可分；特别是按背景支持量条件化后的 AUC 远低于随机。它同时未通过四个核心门槛，不能作为
M26 的补偿输入、残差速度通道、attention 引导或后处理依据。

结论：**停止 M92 的全局背景运动补偿路线。** 不调整池化尺度、相关窗口、速度范围、时间上下文或阈值
尝试翻转这次结论。实现和完整报告：
`log/analysis/analyze_m92_global_background_residual.py`、
`log/analysis/m92_global_background_residual_report.json`。

## M91：H/4 目标级时空联合监督（2026-08-15，不采用）

M91 针对当前约 102 个剩余目标时间窗漏检中高速、小目标的原始低分问题。它从正式 M26 epoch 003
初始化，保留完整事件流、双向 ConvGRU、attention、DACC 和已有 target-flow；新增但**不回写事件 logits**
的三项训练期辅助头：目标中心热图、每时间窗目标存在性和相邻时间窗目标中心位移。训练监督可使用
`target_id`，推理时辅助头不被读取，推理/后处理不使用标签、目标 ID、视频名或验证集调优规则。

初始全分辨率版本在 `99` 视频 `x 8` views 上运行超过 60 分钟而没有完成 checkpoint，已终止。随后将三个
辅助图固定到 H/4（`32x32`）网格，并将标注质心、Gaussian 参数和位移监督同步缩放；随机输入检查确认
启用/不请求 M91 辅助输出时事件 logits 逐元素一致，M26 加载时严格只允许 12 个 M91 新参数缺失。

固定短筛只训练 `event_count > 30000` 的 54 个视频，每视频 2 个视图，共 108 sequences，seed 91、1 epoch；
主干/ConvGRU/attention 学习率为 M26 的 `0.25x`，flow 与新辅助头为 `4x`，中心/存在性/位移损失权重固定为
`0.025/0.010/0.010`。训练耗时 6 分 58 秒，累计得到 2,046 对相邻目标位移监督，三项平均 loss 为
`1.09315 / 1.13919 / 0.08986`。checkpoint：
`log/m91_target_level_h4_m26e3_dense2_e1_seed91/runs/20260815-145203_seed91_pid3948/epoch_001_seed91.pt`。

按完整固定 M10 低密度路由、M91 高密度路由、P41 `0.75/0.25`、P6 `0.718/0.7226`、P0/P0c 和 P18-global
验证：

| 配置 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: |
| 正式基线 | **0.9638562171** | **0.9785804284** | **0.9423022866** | **0.9763227701** | 4.6632902944e-06 |
| M91 H/4 e1 seed91 | 0.9630970005 | 0.9766904662 | 0.9420875311 | 0.9759563804 | **4.6366259916e-06** |

Score 下降 `0.0007592166`，Pd 下降 `0.0018899622`；Fa 的微小改善和 Score_Fa 提升不足以补偿召回损失。
该方向未达到预注册的 `Score +0.0005` 且 Pd 不降门槛。结论：**不增加 epoch、不换 seed、不扫损失权重，
不接入正式 checkpoint。** M91 代码默认关闭，仅保留作为已证伪的联合目标级监督实现和追溯依据。

## M93：跨视频真实目标轨迹复制粘贴增强（2026-08-15，不采用）

M87 只在原视频中沿观测方向平移一个真实目标，因此目标与其背景的共现关系没有变化。M93 改为仅在训练期
将 donor 训练视频中一个连续目标轨迹的真实事件（`x/y/t/polarity`）复制到另一 base 训练视频的时间序列：
保留 donor 的极性、目标局部形状和每个 50-unit 时间箱内相位，将其平移到 base 的可见背景上；原 base
事件、标签和 target id 保持不变。新轨迹必须至少有 3 个连续时间箱，完整落在传感器内，并与 base 已标注
正目标保持 1 像素以上距离。训练后模型与推理只读取普通事件流，不依赖视频名、标签或 target id。

先在训练集做固定可行性审计：99 个训练视频中 54 个高密度视频均有可用连续轨迹；在每视频最多 16 个均匀
窗口、每候选 32 次固定放置尝试下，得到 `1,297` 个轨迹候选和 `40,006` 个无冲突位置。完整放置率为
100%，在传感器内的无正目标冲突率为 96.39%，通过“至少 40 个高密度视频、至少 1,000 个位置、90% 内置率、
80% 无冲突率”的全部准入门槛。因此该方向的数据构造可行，但该审计不是模型收益证明。

实现只新增训练期视图：M87 的原始 `2` 基础视图和 `event_count > 200000` 的 `8x` dense views 保持不动；
额外为每个 `event_count > 30000` 视频附加 `2` 个 copy-paste 候选视图，固定启用概率为 `0.50`。从正式 M26
epoch 003 初始化，保持 BCE、attention、M26 target-flow、P41/P6/P0/P0c/P18 的原参数；seed 93、1 epoch，
共 `516` 序列。训练耗时 12 分 29 秒，实际成功粘贴 `63` 个视图。纯函数和真实训练集日程 smoke 都验证了：
基础视图不变、donor 箱内时间相位保留、无可移动位置时拒绝、合成 target id/标签有效。

完整 `test2.py` 验证固定 M10 低密度路由、M93 高密度路由和正式 P41/P6/P0/P0c/P18-global：

| 配置 | Score | Pd | IoU | Acc | Fa |
| --- | ---: | ---: | ---: | ---: | ---: |
| 正式基线 | **0.9638562171** | **0.9785804284** | **0.9423022866** | **0.9763227701** | 4.6632902944e-06 |
| M93 e1 seed93 | 0.9630798729 | 0.9766904662 | 0.9421376586 | 0.9761090875 | **4.6514394931e-06** |

M93 的 Score 下降 `0.0007763442`、Pd 下降 `0.0018899622`；Fa 仅微降，无法补偿目标时间窗漏检。它同时未达到
预注册的“Score 至少 `+0.0003`、Pd 不降、Fa 不明显增加”任一核心条件。结论：**停止跨视频目标轨迹复制
粘贴路线。** 不调整概率、额外视图数、donor 路由、碰撞半径、seed 或 epoch；也不将它与 M87 叠加。相关文件：
`dataset/cross_video_copy_paste.py`、`log/analysis/analyze_m93_copy_paste_feasibility.py`、
`log/analysis/m93_copy_paste_feasibility.json`、`log/analysis/smoke_m93_copy_paste_dataset.py`、
`log/m93_crossvideo_copypaste_m26e3_e1_seed93/`。

## M94：成功/失败机制复盘与绝对坐标 OOF 诊断（2026-08-15，不采用）

在继续增加新模块前，重新按“为什么真的提分”而不是按实验编号审视了全部历史。正式基线仍为
M10/M26 事件数路由 + P41 + P6/P0/P0c/P18-global，`Score=0.9638562171`。

已确认的稳定增益有共同边界：高密度多视图与事件数路由补足了原始输入覆盖；M20/M26 在严格零扰动挂接后仅
于早期 checkpoint 出现小的有效峰值；P18-global 以极保守的单事件轨迹恢复补到高密度快速目标；P41 则用固定
半时间箱相位平均修正 50-unit 分箱边界。这些方法都依赖测试时可观察的输入几何/密度，且尽可能保留已校准
概率。相反，M29、M36、M87、M91、M93 等直接改变 M26 主路径的训练尝试都优先损伤 `Pd`；M75/M83/M88 说明
候选排序或局部 AUC 不能替代 Challenge 的低 Fa 组件预算。

M26 是卷积网络，时序帧没有显式绝对 `x/y` 坐标。M36/M72/M74 也只使用置信或局部/边界特征，未直接测试
固定传感器位置先验；P44 空间相位 TTA 失败并不等价于这个假设。因此仅在训练集固定
`m75_video_folds.json` 上，对 M75-B 已冻结的候选可观测特征增加固定 `8 x 6` 双线性空间基底，并以严格整视频
留出比较 M26 路由的条件事件 AUC。此诊断只读训练数据和训练标签，不读取公开验证视频，也不改变任何 checkpoint、
阈值或生产掩码。

| outer fold | M26 基础 AUC | 加空间基底 AUC | 增量 |
| ---: | ---: | ---: | ---: |
| 0 | 0.85354 | 0.86973 | +0.01619 |
| 1 | 0.91416 | 0.92385 | +0.00969 |
| 2 | 0.90200 | 0.91473 | +0.01273 |
| 3 | 0.91694 | 0.91087 | -0.00607 |
| 4 | 0.87404 | 0.87119 | -0.00284 |

M10 路由也有 fold 3 AUC/PR 同时退化。由于关键 M26 路由的增益没有跨折同号，且从未达到预注册的
`+0.03` 条件增量门槛，绝对坐标不能视为可迁移的残差证据。结论：**不实现 CoordConv、位置阈值图、热像素
硬掩码或空间残差校准；不对验证集扫描这些规则。**

后续只保留 M95 冻结中心提议器的两外折短筛，详细协议见
`docs/M95_FROZEN_CENTER_PROPOSER_PLAN.md`。它必须从完整画幅独立提出中心、在所有生产后处理完成后仅做单调
OR，并同时满足 Score、Pd、Fa、恢复视频数和新增背景组件门槛；任一外折失败即停止。

## M95：冻结 M26 的独立中心提议器（2026-08-15，已否决）

按 `docs/M95_FROZEN_CENTER_PROPOSER_PLAN.md` 的预注册协议实施，不改动正式 M10/M26+P41+P6/P0/P0c/P18-global 路径。M26 epoch 003 的 decoder `decoded0` 只读输出被平均池化到 H/4 后，接独立的 `Conv3x3(16->16)+ReLU+Conv1x1` heatmap head；M26、DACC、event head、ConvGRU、attention 和 flow 全部冻结。训练仅用训练折标签与 target id 生成 heatmap 监督，推理与候选映射只读取事件流的坐标、时间与极性。

固定筛选为 outer fold 0、calibration fold 1、其余 3 折的 `event_count>30000` 训练视频、seed 95、1 epoch、每视频 2 个确定性视图，共 66 个序列，训练耗时约 44 秒，平均 loss 为 `0.164856`。工程审计全部通过：M95 接口重建的 M26 event logits 与发布路径 `torch.equal`；M26 无可训练参数和无梯度，head 有非零梯度；翻转 label/target id 后，M26 scores、M95 heatmaps 与映射出的原始 event index 均逐元素一致。

calibration fold 的固定阈值集合为 `{0.50, 0.65, 0.80, 0.90}`，并在 P0/P0c/P18-global 完成后仅向 binary mask 单调 OR 原始 event。结果如下：

| heatmap 阈值 | Score 增量 | Pd 增量 | Fa 增量 | 恢复目标窗 | 新增背景组件 | 结论 |
| ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 0.50 | +0.00097799 | +0.00245640 | +2.1160e-08 | 10（4 个视频） | 7 | 否决：`7/10=0.70 > 0.50` |
| 0.65 | 0 | 0 | 0 | 0 | 0 | 无有效恢复 |
| 0.80 | 0 | 0 | 0 | 0 | 0 | 无有效恢复 |
| 0.90 | 0 | 0 | 0 | 0 | 0 | 无有效恢复 |

因此 M95 没有任何满足 calibration 预算的固定阈值，按协议**不读取 outer fold 0 标签、不执行 fold 2、不增加 epoch、不换 seed、不扫描候选数、映射距离、heatmap 半径或公开验证集**。这条路线表明独立中心 head 具备召回残余目标窗的信号，但其无标签候选精度尚不足以满足当前极低 Fa 预算，不能接入正式方案。

相关实现与结果：`model/frozen_center_proposer.py`、`train_frozen_center_proposer.py`、`utils/frozen_center_proposer_inference.py`、`log/analysis/eval_m95_center_proposer_oof.py`、`log/analysis/m95_outer0_oof_report.json`。

## M96：冻结 M95 候选核验器 OOF 探针（2026-08-15，否决）

M95 的 calibration 结果显示独立中心提议器可能包含少量可恢复目标，因此固定 M95
`heatmap threshold=0.50`，不改变 M26/P41/P6/P0/P0c/P18 正式路径，只对尚未被正式 mask
覆盖的 M95 候选提取测试时可观察特征：heatmap 峰值、事件分数、局部事件密度与极性、局部
分数统计、正式 mask 支持以及相邻时间箱候选距离。训练折标签只用于离线拟合一个标准化
Logistic probe，calibration 折固定接受阈值为正候选概率的 20% 分位点，outer 折完全留出。

完整回放使用 outer fold 0、calibration fold 1。候选级 AUC 表面上较高，但由于目标候选极少，
不能替代 Challenge 时间窗级指标：

| 折 | 候选数 | 正候选数 | ROC-AUC | Score 增量 | Pd 增量 | Fa 增量 | 恢复目标窗 | 新增背景事件 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| calibration | 558 | 4 | 0.97383 | -0.00018726 | 0 | +5.6427e-08 | 0 | 18 |
| outer 0 | 435 | 6 | 0.96387 | -0.00024993 | 0 | +7.3132e-08 | 0 | 19 |

outer 中核验器接受了 4 个目标事件和 19 个背景事件，但没有一个完整漏检目标时间窗被恢复，
因此 IoU 和 Fa 均恶化。结论是：**事件级候选 AUC 与目标时间窗级可用性不一致，M96 候选核验器
路线停止。** 不继续训练 verifier、不跑 outer fold 2、不扫描接受阈值、不换 Logistic 配置，
也不将该探针接入正式流程。诊断脚本和原始报告保留在
`log/analysis/eval_m96_candidate_verifier_oof.py`、`log/analysis/m96_candidate_verifier_outer0.json`。

## M97：输入扰动稳定性过滤（2026-08-16，否决）

M97 对正式 M10/M26 + P41 + P6 + P0/P0c 的已保留组件进行三次固定 90% event thinning，
仅保留在至少 2/3 个子流中仍出现的组件。该规则只读取事件流、冻结模型分数和组件几何，
不读取视频名、target id 或验证标签；组件成员资格也通过标签翻转审计保持不变。

完整 99 个训练视频的五折 OOF 结果：

| 指标 | 结果 |
| --- | ---: |
| Score 增量 | -0.00067894 |
| Pd 增量 | -0.00345372 |
| Fa 增量 | -2.38862049e-07 |
| 删除组件 | 76 |
| 删除目标组件 | 40 |
| 删除背景组件 | 36 |
| 通过折数 | 0/5 |

五个 outer fold 的 Pd 均下降。虽然 Fa 略降，但删除的真实目标组件多于背景组件，说明小目标
对事件稀疏扰动的敏感性高于背景伪警，稳定性不能作为当前组件的可靠质量指标。M97 路线停止：
不再扫描保留比例、随机种子、重复次数或宽松门限，也不接入正式推理流程。完整报告保留在
`log/analysis/eval_m97_stability_filter_oof.py` 和 `log/analysis/m97_stability_filter_oof.json`。
